# ============================================================================
# FIXTURE — SYNTHETIC. NOT A HUMAN ARTIFACT.
# Exists only so agent-owned done-conditions can fail honestly while the
# human-gated tasks (T1/T2/T8/T14/T18) are outstanding. Build-plan S5 /
# defect 33. No pipeline path may read this file; tests/test_fixtures.py
# asserts that.
# ============================================================================
# The UniProt accessions below are REAL, and deliberately so: T19 verifies each
# against the live UniProt API (taxonId 9606 + gene symbol), which a fake
# accession could not exercise. The RATIFICATION is the synthetic part -
# `ratified: true` here is a fixture flag, never a human ratification. The real
# configs/barrier_panel.yaml is ratified only by H, in T8.
# HAPPY PATH for T9.

# `face` VALUES ARE SCHEMA FILLER, NOT A REFERENCE. They MIRROR build-plan T7's
# interface block and are NOT attested by anyone. configs/barrier_panel.yaml is the
# authoritative panel once T8 ratifies it. TFRC is `basolateral` here because T7
# prescribes it as of r2.3 (CTO ruling N1, dispatch #18) — this file tracks the PLAN,
# never a biological claim of its own. test_fixture_face_agreement_with_live_panel
# enforces agreement with the live config. Never copy a face from here into a model
# or a test that cares about transport direction.
ratified: true
ratified_by: "FIXTURE - not a real ratification"
ratified_on: "1970-01-01"
panel:
  - {symbol: ABCB1, uniprot: P08183, alias: "P-gp / MDR1", face: apical}
  - {symbol: ABCG2, uniprot: Q9UNQ0, alias: "BCRP", face: apical}
  - {symbol: ABCC1, uniprot: P33527, alias: "MRP1", face: basolateral}
  - {symbol: TFRC, uniprot: P02786, alias: "TfR1", face: basolateral}
  - {symbol: FCGRT, uniprot: P55899, alias: "FcRn", face: apical}
  - {symbol: SLC15A1, uniprot: P46059, alias: "PepT1", face: apical}
  - {symbol: SLCO2B1, uniprot: O94956, alias: "OATP2B1", face: basolateral}
