"""Provenance contract tests — build-plan T11.

Distinct from test_contracts.py: §4.4 reserves that module for the §1.2 *data*
contract test, which arrives with the ChEMBL plan (defect 29).

Populated by T11.
"""

import pytest


@pytest.mark.skip(reason="placeholder — replaced by T11's five provenance contract tests")
def test_provenance_placeholder():
    raise AssertionError("T11 not yet implemented")
