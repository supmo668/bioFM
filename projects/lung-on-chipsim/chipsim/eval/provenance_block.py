"""The model card's data-provenance block.

Populated by T17. The block is built and unit-tested here; wiring it into the
model card belongs to M0c, which owns the card (defect 13). Scaffolded by S11.
"""
