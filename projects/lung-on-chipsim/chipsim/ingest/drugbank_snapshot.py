"""Fetch and parse the pinned public DrugBank 4.2 snapshot (dhimmel/drugbank).

Populated by T3 (fetch), T5 (compound table), T5a (persist), T6 (protein edges).
Scaffolded empty by S11 so those tasks' "(edit)" targets exist.
"""
