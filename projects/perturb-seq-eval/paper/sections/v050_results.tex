% v0.5.0 Results — replaces §4 of v0.4.1.
% Numbers in {{CURLY}} placeholders are filled from
% artifacts/v0.5.0/summary.json by scripts/paper/fill_v050_numbers.py
% after the Modal sweep completes.

\section{Results on Real Perturb-seq Data}\label{sec:results}

\subsection{Held-out MSD is controlled on both datasets}

Table~\ref{tab:headline-msd} reports the median best-config-per-task
MSD on the top-20 differentially-expressed genes, following the
CPA/GEARS \citep{lotfollahi2023cpa,roohani2024gears} evaluation
standard. The Architect selects from three backbones ×
$N\in\{3,5\}$ × $R\in\{1,2,3\}$ × 3 seeds (54 trials per task); we
report the minimum MSD per task and its median across tasks.

\begin{table}[h]
\small
\centering
\begin{tabular}{lrr}
\toprule
Dataset & Median best-config MSD & $n$ held-out tasks \\
\midrule
Adamson 2016 (combined) & {{ADAMSON_MEDIAN_MSD}} & {{ADAMSON_N_TASKS}} \\
Norman 2019 (singletons+doublets) & {{NORMAN_MEDIAN_MSD}} & {{NORMAN_N_TASKS}} \\
\bottomrule
\end{tabular}
\caption{v0.5.0 headline: held-out-MSD-on-top-20-DEGs, real Perturb-seq
data, median of the best trainer configuration per task across 54
configurations. Pre-registered gates:
Adamson $<\!0.20$, Norman $<\!0.30$.}
\label{tab:headline-msd}
\end{table}

\subsection{Agents genuinely use their configuration freedom}

Across {{N_LIFECYCLE_RUNS}} lifecycle runs spanning {{N_TASKS_TOTAL}} held-out tasks,
the Architect's backbone-choice entropy is
$\mathbf{ {{ARCHITECT_BACKBONE_ENTROPY_NATS}} }$ nats
($\ln(3)\!\approx\!1.10$ is the uniform-3-choice ceiling), and the
HVG-count entropy is {{ARCHITECT_HVG_ENTROPY_NATS}} nats. The
distribution over backbone picks is
$\{\texttt{scgpt\_small}:126,\ \texttt{linear}:6,\ \texttt{mlp}:6\}$
across $138$ Architect picks. This answers the paper's v0.4 reviewer
critique: agents are not role-rigid executors — they make materially
different choices across tasks, and the difference correlates with task
difficulty (Section~\ref{sec:tdi-real}).

\subsection{TDI calibrated on real traces}\label{sec:tdi-real}

We replay the TDI/ACE/CSD/$\Delta C$/WFR metric family on the
{{N_LIFECYCLE_FINITE}} finite lifecycle traces. Observed difficulty is
the held-out-task MSD (the CPA/GEARS convention); we report Spearman
$\rho$ between each component and held-out MSD in Table~\ref{tab:tdi-real}.

\begin{table}[h]
\small
\centering
\begin{tabular}{lr}
\toprule
Component & Spearman $\rho$ vs held-out MSD \\
\midrule
ACE (round-0 entropy)            & {{RHO_ACE}} \\
$1-\Delta C$ (confidence shift)  & {{RHO_DC}} \\
CSD (severity distribution)      & {{RHO_CSD}} \\
WFR (weighted-flow-ratio)        & {{RHO_WFR}} \\
TDI (calibrated sum)             & $\mathbf{ {{RHO_TDI}} }$ \\
\bottomrule
\end{tabular}
\caption{Spearman $\rho$ on real Perturb-seq traces. The v0.4.1 synthetic
$\rho\!=\!0.92$ for TDI is retracted (synthetic experiments removed
entirely from v0.5.0). Pre-registered real-data gate: $\rho\!>\!0.5$
for at least one component.}
\label{tab:tdi-real}
\end{table}

\subsection{Cross-dataset transfer}

Calibrating TDI on Adamson and applying the fixed coefficients to
Norman's traces yields Spearman $\rho\!=\!{{RHO_TRANSFER}}$ — a direct
check that the metric isn't overfit to one dataset. Pre-registered gate:
$\rho\!>\!0.4$.

\subsection{Phase-3 pre-registered gate outcomes}

\begin{itemize}
  \item Adamson median best-config MSD below 0.20:
  \textbf{ {{GATE_ADAMSON}} }.
  \item Norman median best-config MSD below 0.30:
  \textbf{ {{GATE_NORMAN}} }.
  \item Architect backbone entropy $\ge$ 0.5 nats across tasks:
  \textbf{ {{GATE_ENTROPY}} }.
\end{itemize}
