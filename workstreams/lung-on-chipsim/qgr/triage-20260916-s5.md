# §5 (r2.19 G-15 + G-18) — TRIAGE. Base 83188df, Hash A 8330478. Threshold 80.

FULL roster (code, security, design, test) + own review, per the CTO's standing rule: this diff
touches an interface and the human artifact's write path.

FIXED IN THIS GATE (84ce8e0) — every one verified by a mutation that now fails:
Q5-01 [98] CASE BYPASS, executed by two reviewers: `CONFIGS/` allowed, file landed in the REAL
  configs directory with `name` at position 2. The guard was defeated by one shifted keystroke.
Q5-02 [96] CHECK-ONE-OBJECT / WRITE-ANOTHER, executed: the check resolved, the write used the
  literal path, and `os.replace` REPLACED a destination symlink with a real name-bearing file
  inside the tracked directory.
Q5-03 [92] SYMLINKED `configs` DIRECTORY, executed: resolve() erased the component, the write went
  through the link. Resolving — which I had documented as the strengthening — was the defeat.
Q5-04 [90] OVER-BREADTH: any `configs` component anywhere in an absolute path refused EVERY
  worksheet write, including the legitimate data/interim/ one and the recovery path. Now anchored
  to source_root() with the component check applied relative to the project root.
Q5-05 [88] SYMLINK LOOP raised RuntimeError, escaping every `except AdjudicationError` including
  the CLI handler's — the traceback that handler exists to prevent.
Q5-06 [88] FALSE RECOVERY INSTRUCTION: the G-01 refusal told the human to recover via a function
  that cannot do it, and G-15 had just closed the last workaround. The message now states the
  truth: the tracked file IS the surviving copy, there is no automatic recovery, here is what to
  do instead.
Q5-07 [86] CLI ERROR CONTRACT: a transposed letter in --worksheet gave a 25-line pandas traceback
  and exit 1. Now a message and exit 2, pinned by tests over missing / empty / directory inputs.
Q5-08 [85] MY TESTS' `match="configs"` WAS SATISFIED BY THE DESTINATION PATH. Proven by a mutant
  pair: a bare-path message SURVIVED, an unrelated "permission denied" message echoing the path
  SURVIVED, and the CORRECT pathless message was KILLED — the assertion actively rejected a
  correct message. Now matched on the rule, with the path asserted separately.
Q5-09 [85] THE RESOLUTION TEST DID NOT TEST RESOLUTION: both a no-resolve and a naive substring
  implementation survived the whole suite. Now uses a bare filename written from inside configs/,
  the only shape that separates them.
Q5-10 [84] JOURNAL POLLUTION: the CLI tests wrote GENUINE invocation records into the project's
  real trail on every run — the trail the repo leans on as evidence a human ran something.
  Redirected, and the record is now asserted to land in the scratch root (38 before, 38 after).
Q5-11 [83] PARENT-ONLY COVERAGE: every refusal test put the file directly in configs/, so a
  `parent.name == "configs"` implementation survived. "Anything UNDER" is now tested.
Q5-12 [82] OVER-REFUSAL UNTESTED: a substring implementation survived the narrowness test.
  Parametrised over myconfigs_backup / my_configs_archive / config / configs2.
Q5-13 [82] The refusal exit code was unpinned (`!= 0`), the success output asserted nothing (a
  deleted line and a WRONG COUNT both survived), and the fail-closed consequence was untested.
Q5-14 [80] Stale "worksheet" wording r2.19 fixed in the plan but not the code; the public
  docstring did not name its own first refusal; TRACKED_DIR_NAME was public, generically named and
  carried a false exclusivity claim.

ESCALATED — real, and NOT mine to decide (the plan is the CTO's; these widen scope):
Q5-15 [HIGH] The refusal protects a directory NAME, not TRACKED-NESS. Measured: the name-bearing
  worksheet writes cleanly to the project root, docs/, tests/fixtures/ and workstreams/ — all
  tracked. Guarding tracked-ness needs a ruling on how (an allow-list of writable roots is the
  obvious shape, and it is a bigger interface change than r2.19 authorises).
Q5-16 [HIGH] A SECOND UNGUARDED WRITER with a worse payload: `write_compounds` persists accession
  + name + InChI + InChIKey on one row — the complete record — and `chipsim write --out` takes an
  unvalidated destination. Measured writing into configs/. This is T5a's interface, not T14's.
Q5-17 [MED] The repo-wide guard SKIPS non-UTF-8 files, so a tracked parquet carrying the full
  association is invisible to it; and there is no `name` detector at all, so the name half of the
  invariant has exactly ONE enforcement point in the repo.
Q5-18 [MED] FAIL-CLOSED BY INHERITANCE: `adjudication-export` got panel-seal's fail-closed
  journalling because both live in the tuple a workflow test requires, not because anyone chose
  it. I KEPT the behaviour (it is the safe direction, and security judged it harmless since the
  export is idempotent and publishes nothing on failure) and now TEST it — but no clause states
  it, and changing it would be equally unstated. Ruling wanted.
Q5-19 Plan amendments for r2.20: T14's Files clause still says "move to configs/" — the hand-move
  its own r2.19 block calls the accident; T13's interface and Done-when never mention the refusal
  that now guards T13's own writer; and several behaviours (the already-projected refusal, the two
  parquet readers, the tracked-dir constant) appear in no Interfaces block.
Q5-20 [DISCLOSURE] The project's real invocation journal now holds 38 `adjudication-export`
  records written by test runs (4 mine pre-review, ~34 from reviewer verification). Git-ignored,
  so nothing is committable and nothing is redistributed — but the trail is not pristine. Both
  reviewers declined to delete audit-trail entries on their own authority and so do I; the CTO
  should decide whether to purge them.

PROCESS:
Q5-21 [HIGH] A reviewer again MUTATED THE SHARED WORKING TREE mid-gate — third occurrence today,
  and this time I caught `.resolve()` stripped from the live file WHILE two reviewers were still
  running. Nothing was compromised (Hash A was taken from the committed state, and I verified the
  tree byte-identical before hashing), but the protocol is evidently not enough on its own. My
  rule now: never hash, never commit, never push while a reviewer is live; verify `git diff` is
  empty first, every time.
Q5-22 [GOOD] Red-first worked. All four new tests were observed failing for the right reasons
  before implementation, and a reviewer independently confirmed it by running the new tests
  against the parent commit. The mutants that still survived were about test POWER, not existence
  — which is the failure red-first cannot catch and mutation can.
