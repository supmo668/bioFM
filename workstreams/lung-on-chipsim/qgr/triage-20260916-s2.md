# §2 quality gate — TRIAGE dispositions

F-01 ACCEPT — fix in this gate. Verdict aggregated over ALL member names per key: any "disagrees"
  wins; "agrees" only if at least one member agrees and none disagrees; else "unresolved".
  Bug-exposing test: two rows (L-Proline, D-Proline) on the L key must yield "disagrees".
  Also: correction to CTO — #130 claimed no code path could produce a false "agrees"; that was false.
F-02 ACCEPT — test the flag on add_canonical_identity_excluding's kept AND excluded frames.
F-03 ACCEPT — reference table: neutralise/replace the two charged keys; loader rejects duplicate
  base_name and l_inchikey == d_inchikey, and requires neutral (-N) keys; add a test that loads the
  committed table.
F-04 ACCEPT — test: a /s2 string that also carries /b keeps its /b geometry after the strip.
F-05 ACCEPT, CANNOT FIX IN THIS DIFF — plan-level (T14 destination). Escalated to the CTO for a ruling;
  disclosed in the receipt. Not silently deferred.
F-06 ACCEPT — roster reports label disagreements (listing, not rejecting) via the committed reference.
F-07 ACCEPT — live scan uses `git ls-files -z`, splits on NUL; unresolvable tracked paths fail the test
  except submodule gitlinks, which are skipped explicitly; anti-vacuity assertions on the tracked list.
F-08 ACCEPT — planted-positive ledger test (and non-ledger negative); window edge (2 caught, 3 not),
  before/after direction, InChIKey-only case.
F-09 ACCEPT — README module table + limits text; build-plan T13/T15 interface blocks flagged to the
  CTO (plan is CTO-owned and hash-locked).

DROPPED F-10..F-31: recorded with scorer reasons, not fixed in this gate. F-13 (per-row citation) and
F-14 (coined names quoted in the reference YAML) raised to the CTO for rulings.
