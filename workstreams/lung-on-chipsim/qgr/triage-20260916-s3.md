# §3 (T14/T15 r2.18) — TRIAGE. Base 0c4e181, Hash A 3e6ce53. Threshold 80.

ACCEPT — fixed in this gate:
G-01 [95] CODE-2/SEC-8 export clobbers a filled tracked file from a blank worksheet, returning a
  row count that reads as success. VERIFIED BY ME: 24 filled verdicts -> 0, returned 24. The
  never-clobber rule (defect 22) must cover the tracked file too. Fix: refuse to blank a cell an
  existing `out` has filled; allow a projection that only adds.
G-02 [92] TEST-1/CODE-3/DES-9 the flag's key alignment is untested; positional assignment keeps the
  whole suite green (mutation-proven by two reviewers independently). Implementation is CORRECT —
  this is a test gap on the one line that binds flag to row. Fix: reversed-order test.
G-03 [90] OWN-1/CODE-5 a non-boolean `stereo_is_relative` column is coerced by truthiness, so
  strings "False"/NaN report 24 of 24 compounds relative. VERIFIED BY ME. Fix: refuse a
  non-boolean flag column in BOTH T13 and T15 rather than coercing.
G-04 [88] TEST-2/CODE-4 atomicity of the export is unpinned; removing tmp+os.replace keeps the
  suite green (mutation-proven). Fix: to_csv raises mid-write, assert `out` bytes unchanged.
G-05 [88] DES-11 no test asserts the FIXTURE key namespace for the adjudication fixtures, although
  S5 exempts CSVs on exactly that ground — and the tracked fixtures are the first fixtures
  schema-identical to the real tracked artifact. Fix: assert every key starts with FIXTURE.
G-06 [85] SEC-2 the five-column rule is enforced only at read time. Fix: a fail-closed test — IF
  configs/pgp_adjudication.csv exists, its header is exactly TRACKED_COLUMNS.
G-07 [85] TEST-3 nothing pins that compounds may be BROADER than the file (the normal pipeline
  shape); an injected refusal kept the suite green. Fix: test with a surplus flagged key.
G-08 [85] OWN-4 my own idempotency test passed VACUOUSLY off the tmp_path directory name. Fix the
  assertion, and make the export say "already the tracked projection" (DES-7/TEST-7).
G-09 [84] DES-3 the refuse-vs-drop guard is keyed to WRITTEN_COLUMNS while the drop set is
  WRITTEN_COLUMNS - TRACKED_COLUMNS: a future column would be dropped silently. Fix: derive
  TRACKED_COLUMNS from HUMAN_OWNED_COLUMNS and pin the drop set against the docstring.
G-10 [83] CODE-6/DES-6 T15 raises a bare KeyError where T13 raises AdjudicationError for the same
  missing `canonical_inchikey`. Fix: mirror T13's guard.
G-11 [82] DES-5 T15's messages say "worksheet" while naming the TRACKED path, pointing the reviewer
  at hand-editing the file the split exists to protect. Fix: rename the local, point the remedy at
  the worksheet + re-export.
G-12 [82] TEST-4/TEST-5 two refusal tests match strings common to both refusal branches, so a
  wrong-diagnosis refusal passes. Fix: assert the listed columns / the branch-specific phrase.
G-13 [80] TEST-6 header-only tracked file and zero-row export unexercised. Fix: pin both.
G-14 [80] DES-14 `ruff format --check` fails and the README declares it part of the gate. Fix MY
  three files; disclose the five pre-existing ones (out of scope, not silently reformatted).

ACCEPT, CANNOT FIX HERE — escalated to the CTO (plan is hash-locked):
G-15 SEC-1 write_adjudication_worksheet will write `name` to ANY path, including a tracked one
  (reviewer demonstrated it against configs/pgp_adjudication.csv). Making the export the only writer
  of a tracked path needs a policy call: a library function shelling out to `git ls-files` is the
  obvious check and not obviously acceptable. NO non-test caller exists today.
G-16 DES-1 T15's "Done when" still says "worksheet" three times (r2.18 changed the interface, not
  the neighbouring clause — the same class as the defect r2.18 repaired).
G-17 DES-EXTRA the extra-column refusal is unstated in the plan; reviewer judged it justified.
  Record as an r2.19 clause rather than code-only behaviour.
G-18 DES-16 T14 is a HUMAN task whose helper has no CLI entry, against the repo's own `panel-seal`
  precedent (C4). A new subcommand is not authorised by r2.18.
G-19 DES-15 read_pgp_labels drops the flag T15 writes "so T17 receives it" — new API surface.
G-20 SEC-3 a compound name typed into a CELL (adjudicated_by / evidence_doi) still reaches the
  tracked file; unreachable by a column check. Belongs in T14's human instructions (CTO file).

DROPPED (<80), recorded not fixed:
SEC-4 no CLI (= G-18, escalated instead) · SEC-5 mkdir on caller-supplied path (developer-supplied)
· SEC-6 the .tmp lands in configs/ with no .gitignore rule covering it (contents are the safe five
columns; hygiene) · SEC-7 CSV formula injection (pre-existing pattern, whole-project concern)
· CODE-7/DES-8 unreachable `missing` branch · CODE-8/TEST-8 __doc__-only test (superseded by G-09,
which gives it teeth) · CODE-9/TEST-9 near-duplicate test · CODE-10 EmptyDataError on a zero-byte
file (same as the pre-existing reader) · DES-4/12 comment-only taxonomy and pointer
· DES-13/TEST-10 vestigial tmp_path args and "worksheet" nouns in migrated test names (cosmetic;
folded into G-11 where the wording actually misleads) · DES-17 docstring/test-helper tidying.

REJECTED:
CODE-1 [NOT REPRODUCIBLE] "an uncommitted edit deleted the atomic publish". Checked on receipt:
  `git diff` empty, the tmp+os.replace block present on disk and in 61da71b, tree clean. Explained
  by OWN-3: reviewers ran mutation tests against the shared working copy. Recorded, not actioned.

PROCESS (reported to the CTO, not a code change):
OWN-3 two reviewers mutated the tree the gate was hashing. Mutation testing proved three real gaps
  (G-02, G-04, G-07) and is worth keeping — but it must run on a copy. A mutation left behind would
  be signed into a receipt as if it were the reviewed state.
