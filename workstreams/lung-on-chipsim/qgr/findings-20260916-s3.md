# §3 (T14/T15 r2.18) — own review findings, base 0c4e181, Hash A 3e6ce53

OWN-1 [HIGH] adjudication.py — a STRING flag column silently makes every compound relative.
  `.astype({"stereo_is_relative": bool})` (write_adjudication_worksheet, line ~191) and
  `.astype(bool)` (adjudicate_pgp_labels, the new relative_by_key + the parquet write) treat any
  NON-EMPTY string as True. Probe: compounds with stereo_is_relative = ["True", "False", "False", ...]
  produced [True, True, True] in pgp_labels.parquet — the exact stale/false-flag failure the
  generated-column class exists to prevent, and T17 would receive it as fact.
  Reachable: any compounds frame that has been round-tripped through CSV (dtype=str is the
  project's own convention for reading these files), or built by hand in a notebook.
  Pre-existing in the T13 path; the r2.18 T15 path inherits it. Fix: refuse a non-boolean flag
  column rather than coercing (consistent with "refuse rather than guess"), or map the strings
  explicitly. Needs a red test in BOTH paths.

OWN-2 [LOW] adjudicate_pgp_labels — a header-only tracked file raises "wholly unadjudicated".
  True, but it describes the wrong cause: the file has no rows at all. A distinct message
  ("carries no rows") would point at the real problem. Cosmetic; message-only.

Probes that came out CLEAN (recorded so the gate is falsifiable, not just assertive):
  - compounds in a different order than the file: the flag follows the KEY, not the row position.
  - compounds carrying extra keys the file lacks: allowed, 24 file rows in, 24 out.
  - verdict-to-key mapping verified row-for-row against the file read independently.
  - export with an extra human column: refused, and NO partial file written.

# reviewer-security (verdict: no record-content exposure introduced; fixtures read in full, clean)
SEC-1 [MED] write_adjudication_worksheet validates compounds but never `out`: it will write
  WRITTEN_COLUMNS (name at position 2, beside canonical_inchikey) to ANY path, including a tracked
  one. Reviewer ran it against configs/pgp_adjudication.csv and it created the file with `name`.
  No non-test caller exists today, so operator-reachable only. Recommend: refuse a git-tracked
  `out` (or require data/interim/), making export_tracked_adjudication the ONLY writer of a
  tracked path — that asymmetry is what makes the r2.18 split load-bearing.
SEC-2 [MED] The five-column rule is enforced only at READ time, and the repo-wide guard has no
  name-detection at all. A human doing T14 the manual way ("move to configs/", still the plan's
  wording) could commit a worksheet-shaped file with `name` and the whole suite + guard stay green.
  Recommend a fail-closed test modelled on test_merge_report_record_content.py: IF
  configs/pgp_adjudication.csv exists, its header is exactly TRACKED_COLUMNS.
SEC-3 [LOW] Projection is schema-level, not content-level: a compound name typed into
  adjudicated_by / evidence_doi / adjudicated_on passes through into the tracked file beside the
  key. Not fixable by a column check — belongs in the T14 human instructions (CTO-owned file).
SEC-4 [LOW] export_tracked_adjudication has no CLI / `python -m` entry, so the human's path of
  least resistance is still hand-deleting columns — the accident the helper exists to prevent.
SEC-5 [LOW/INFO] out.parent.mkdir on a caller-supplied path (developer-supplied, not attacker).
SEC-6 [LOW] The atomic temp is `configs/pgp_adjudication.csv.tmp`; no .gitignore rule covers
  configs/** or *.tmp, so a hard kill mid-write leaves a file `git add -A` would stage. Contents
  are the safe five columns — hygiene, not exposure.
SEC-7 [LOW] CSV values written unsanitized: a cell starting = + - @ is a formula in Excel.
  Pre-existing pattern, extended by the new writer.
SEC-8 [non-security, integrity] export_tracked_adjudication overwrites `out` unconditionally — no
  never-clobber counterpart to the worksheet's defect-22 merge. Re-exporting from a STALE worksheet
  silently replaces a more complete tracked file holding irreplaceable human work.

# reviewer-code (no pre-existing validation lost in the migration; flag alignment verified correct)
CODE-1 [REJECTED — NOT REPRODUCIBLE] Claimed an uncommitted working-copy edit had deleted the
  atomic publish from export_tracked_adjudication. Checked immediately on receipt: `git status`
  clean except config/monitor-pids.json (not mine), `git diff` on the file EMPTY, and the tmp +
  os.replace + finally-cleanup block present both on disk and in 61da71b. No such edit exists or
  was committed. Recorded as rejected, not silently dropped. Its derived observation (CODE-4) is
  kept on its own merit.
CODE-2 [MED] = SEC-8, now with a worked failure: export clobbers `out` unconditionally and does no
  content validation, so a BLANK worksheet overwrites a fully adjudicated tracked file and returns
  24 (reads as success). Path: data/interim/ worksheet lost (clean checkout / DVC re-pull / new
  machine) -> T13 regenerates it blank without error (the vanished-key guard only fires when the
  PRIOR worksheet carried human content) -> reviewer re-exports "to refresh configs/" -> 24 empty
  rows. T15 then raises, but after the fact.
CODE-3 [MED] No test distinguishes KEY-aligned from POSITIONAL assignment of the recomputed flag:
  all six tracked fixtures are already in sorted key order, groupby returns sorted keys, and the
  test compounds frames are built in fixture order — so the three orderings coincide everywhere.
  Dropping the `.reindex(...)` would pass the whole suite. (Implementation IS correct — my own
  probe reversed the order and the flag followed the key — but the property is untested.)
CODE-4 [MED] test_export_leaves_no_temporary_file_behind asserts only the RESIDUE (tmp_path
  contents), which a plain to_csv with no atomic write satisfies identically. Assert the
  mechanism: fail mid-write against an existing `out` and check its bytes are unchanged.
CODE-5 [LOW] = OWN-1 (string/NaN flag coerced to True). Adds: r2.17's T15 parsed the flag as
  == "true" and handled the string form correctly, so this hazard is NEW to T15 (pre-existing in
  T13 and ids.py). A nullable-boolean column holding pd.NA raises a bare ValueError, not
  AdjudicationError.
CODE-6 [LOW] T15 guards compounds for stereo_is_relative but NOT for canonical_inchikey: a frame
  keyed "inchikey" dies with a bare KeyError, while write_adjudication_worksheet raises a clear
  AdjudicationError for the same mistake. The two entry points disagree.
CODE-7 [INFO] The `missing` check inside export_tracked_adjudication is unreachable (_read_worksheet
  already requires a superset). Harmless but reads as a live guard.
CODE-8 [INFO] test_export_names_the_columns_it_drops asserts only on __doc__ — a documentation
  lint. Behaviour is covered by the neighbouring test, so redundancy, not a hole.
CODE-9 [INFO] test_t15_flag_comes_from_compounds duplicates the extra-column refusal test and does
  not demonstrate the property in its name (that property is CODE-3's gap).
CODE-10 [INFO] A zero-byte tracked file surfaces pandas EmptyDataError, not AdjudicationError —
  same as the pre-existing _read_worksheet, so no regression.

# reviewer-design (conformance clean on substance; composition coverage present)
DES-3 [HIGH] The export's "refuse rather than drop" guard tests membership of WRITTEN_COLUMNS while
  the real drop set is WRITTEN_COLUMNS - TRACKED_COLUMNS. A future column added to WRITTEN_COLUMNS
  (including a new HUMAN_OWNED one) would be silently DROPPED, not refused — the exact loss the
  helper exists to prevent — and the docstring's hard-coded list would stop being true.
DES-5 [MED] T15's local is `worksheet = Path(adjudication)` and every message says "worksheet"
  while interpolating the TRACKED path, so a reviewer is told to hand-edit configs/pgp_adjudication.csv
  — the very route by which a `name` column reaches it.
DES-6 [MED] = CODE-6 (canonical_inchikey guard parity).
DES-7 [MED] Re-exporting the export's own output fails with "missing column(s): ['name',
  'snapshot_label']" — telling the human to ADD `name` to the file whose design point is to lack it.
DES-10 [MED] The export's test input is a hand-built PROXY for T13's output, so the T13->T14 leg is
  pinned against a shape rather than against write_adjudication_worksheet itself.
DES-11 [HIGH] S5 exempts CSV fixtures from the sentinel banner "covered by digest + key namespace",
  but NO test asserts the key namespace for any adjudication fixture. The tracked fixtures are the
  first fixtures schema-identical to the real tracked artifact, so `cp` + a one-cell edit installs a
  synthetic adjudication as the genuine human artifact. Add the FIXTURE-prefix assertion S5 already
  applies to the snapshot TSVs.
DES-14 [MED] `ruff format --check` FAILS (README declares it part of the gate; the config's
  format_command is empty, so previous gates ran `ruff check` only). 8 files: 3 mine, 5 pre-existing.
DES-15 [MED] read_pgp_labels drops `stereo_is_relative`, so the flag T15 writes "so T17 receives it"
  is unreachable through the module's own reader.
DES-16 [MED] export_tracked_adjudication has no CLI entry, though T14 is a HUMAN task and the repo
  has an explicit precedent (`chipsim panel-seal`, C4). Needs CTO authorisation (new subcommand).
DES-1 [INFO->CTO] T15's "Done when" still says "worksheet" three times; r2.18 changed the interface
  but not the neighbouring clause. Same class as the defect r2.18 repaired. Escalate, do not edit.
DES-EXTRA [INFO->CTO] The extra-column refusal in _read_tracked_adjudication is unstated in the plan.
  Reviewer judged it justified; record it as an r2.19 clause rather than code-only behaviour.
DES-4/8/12/13/17 [LOW] taxonomy comment; dead `missing` branch; S5 pointer comment; vestigial
  tmp_path args + "worksheet" nouns in migrated test names; docstring should name the two columns
  T15 actually uses and helpers should stop building an unused `name`.

# reviewer-test (mutation-verified; migration lost no test)
TEST-1 [CRITICAL, mutation-proven] Replacing the flag's `.reindex(...)` with positional assignment
  left the FULL SUITE GREEN (690/4). Highest-value gap in the change. (Implementation is correct.)
TEST-2 [HIGH, mutation-proven] Replacing the export's tmp+os.replace block with a plain to_csv left
  the suite GREEN. The atomicity claim is unpinned; T13 already has the template
  (test_t13_writes_atomically). Assert via a to_csv that raises mid-write.
TEST-3 [HIGH, mutation-proven] Injecting a refusal for compounds BROADER than the file left the
  suite GREEN: nothing pins that T15 tolerates a compounds frame carrying extra keys — the normal
  pipeline shape.
TEST-4 [MED] test_t15_refuses_a_worksheet_where_the_tracked_file_belongs asserts "name" in message,
  but that word appears UNCONDITIONALLY in the refusal template; and "export_tracked_adjudication"
  appears in BOTH refusal branches. Assert the listed columns instead.
TEST-5 [MED] test_t15_refuses_a_tracked_file_carrying_a_name_column matches a string common to both
  branches, so a wrong-diagnosis refusal passes. Tighten to "outside the tracked schema" + column.
TEST-6 [MED] Header-only tracked file and zero-row export are unexercised (empty any()/value_counts/
  groupby are classic pandas edges).
TEST-7 [MED] Re-export idempotency untested (= DES-7).
TEST-8/9/10 [LOW] __doc__-only test; near-duplicate test; dead helper knob + 11 unused tmp_path args.

# own (process)
OWN-3 [PROCESS] Two reviewers MUTATED the shared working copy during review despite "do not fix"
  (design reviewer observed .reindex removed/restored and .any()->.first(); test reviewer states it
  restored byte-for-byte). This produced CODE-1, a HIGH finding against a file state that never
  existed in any commit. Verified clean: `git diff` empty, HEAD intact. Mutation testing is valuable
  — it proved TEST-1/2/3 — but it must run on a COPY, not the tree the gate is hashing.
OWN-4 [HIGH, own] My own new test test_exporting_an_already_tracked_file... PASSED VACUOUSLY: pytest
  names tmp_path after the test, so the path string contains "already" and my assertion matched the
  DIRECTORY NAME, not the error message. Same class as TEST-4. Fixed to assert the phrase.
