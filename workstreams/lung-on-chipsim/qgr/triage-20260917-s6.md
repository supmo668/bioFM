# §6 (E-3 guard) — TRIAGE. Base 995b5a5, Hash A 7f27339. Threshold 80.

FULL roster (code, security, design, test) + own review, per the CTO's standing rule — this is the
record-content surface. Reviewers worked on a THROWAWAY COPY (the structural fix the CTO ruled);
all four independently confirmed the real tree ended unmodified, and it did. First gate today with
no mutation left behind.

FIXED IN THIS GATE (784d9ad) — 7 of the 16 new tests fail against the pre-fix module:
Q6-01 [98] PARQUET FOOTER METADATA carried the whole record invisibly AND was not reported: schema
  and per-field metadata holding accession + coined name + structure scanned as ",harmless\\n0,1\\n".
  Neither half of the mechanism fired, so the guard certified it clean. This blocked release.
Q6-02 [94] LONG LIST CELLS elided by numpy's repr above 1000 elements — an accession at position
  1500 of a `groups` list vanished, and `write_compounds` persists `groups` and `atc_codes` as
  list columns. The project's own shape, not a contrived one.
Q6-03 [92] PARQUET DISPATCHED ON THE NAME: `UP.PARQUET`, `.pq` and an extensionless blob fell
  through to "cannot be decoded — declare it", and declaring them would make a fully readable
  record carrier permanently invisible. THE SAME CASE-SENSITIVITY SHAPE THIS REPO CLOSED THREE
  COMMITS AGO (84ce8e0). Now dispatched on the PAR1 magic bytes.
Q6-04 [90] LATIN-1 AND UTF-16 TEXT carrying an accession was classified "undecodable" with the
  allow-list as the only exit — which would have made a PLAIN-TEXT carrier invisible for good.
  Now decoded and scanned, while genuine binary (NUL bytes / low printable ratio) stays reportable.
Q6-05 [88] THE LEDGER PAIR was exempt from the readability check although its content IS still
  read by `ledger_tuple_hits` — the exclusions exist for accession CONTENT, not for readability.
  Dispatch payloads stay skipped: waived by ruling and never scanned either way.
Q6-06 [86] RESOURCE HANDLING: one giant string built OUTSIDE the try (30 KiB -> 186.9 MiB
  measured, 6,317x), parsed TWICE per run by the two callers, with MemoryError swallowed by
  `except Exception` and reclassified as "undecodable — declare it". Now: row batches, a size cap
  that REPORTS, MemoryError and ImportError distinguished, and a cached readability verdict.
Q6-07 [85] MY TESTS ACCEPTED A SUFFIX- OR BASENAME-MATCHED ALLOW-LIST (both mutants survived), so
  `vendor/<declared path>` or any file sharing a declared basename would have been exempted —
  against a docstring that claimed "exact path, not suffix".
Q6-08 [85] NOTHING PINNED that the allow-list is a declaration of UNREADABILITY, not a content
  waiver: adding `or rel in BINARY_ALLOWLIST` to the accession scan survived the whole suite.
Q6-09 [84] STALE / SPECULATIVE DECLARATIONS passed everything — a deleted figure, a PRE-DECLARED
  `data/processed/compounds.parquet`, and README.md (decodable text). Now every declared path must
  exist, be tracked, and be genuinely unreadable.
Q6-10 [82] REPORTING could be limited to familiar extensions and pass; deep rows, middle columns
  and the parquet index were unpinned; `sorted()` was unpinned; "empty is read, not skipped" was
  pinned only by accident (six .gitkeep files).

ESCALATED — NOT mine to decide, and the reason is the same one I gave for E-1:
Q6-11 [HIGH] OWNERSHIP. All 24 declared paths belong to OTHER modules, declared inside MY module.
  perturb-seq-eval adds a figure -> my suite goes red -> the repair is an edit to a
  lung-on-chipsim SOURCE file by someone who owns neither. I ESCALATED exactly this class one
  finding earlier (E-1) and then decided it unilaterally here. The ruling authorised "a declared
  binary allow-list"; it did not say where it lives or who owns it. Reviewer's better shape:
  per-project declaration data (the module's own precedent — DRUGBANK_ID_LEDGER points at a
  configs/ YAML rather than inlining), plus a "derived from tracked source S, and S is in scope"
  rule that is self-maintaining and encodes a CHECKABLE claim instead of my unverifiable comment
  "none is a DrugBank artifact".
Q6-12 [HIGH] THE h5ad DOES NOT BELONG IN THAT LIST. It is the only entry that is not a rendered
  artifact, the only one with no tracked generating source, and the only one that is a structured,
  readable, record-shaped container. The ruling's own argument for reading parquet applies to it
  verbatim. Security opened it with h5py (46 nodes, every string dataset and attribute scanned):
  ZERO hits, so it is defensible ON CONTENT today — but I extended "declare and skip" to a second
  READABLE structured format inside the same frozenset, where a reader skims 24 figures and never
  registers it. Either read it (h5py, as parquet got) or separate the two kinds by name.
Q6-13 [MED] CONTENT PINNING. A declaration keys on a PATH, and every declared file is a BUILD
  OUTPUT: regenerate the paper or re-export the AnnData with drug annotations and the guard stays
  silent forever. path -> sha256 would fix it, but that changes the declaration's SHAPE, which is
  the thing I must stop deciding alone while Q6-11 is open.
Q6-14 [HIGH, ruling] AN UNDECODABLE BINARY IN A DISPATCH DIRECTORY is double-exempt and silent —
  proven: a tracked `dispatches/leak.pdf` with raw bytes plus a real accession left the suite
  green. #122 §3 waives dispatches because "redacting a sent MESSAGE falsifies the audit trail"; a
  PDF dropped there is not a message whose text is being audited. I kept the waiver and did NOT
  widen it unilaterally. Ruling wanted.
Q6-15 [MED] THE NAME HALF still has no detector: the parquet path applies only the accession
  regex, so NAME + STRUCTURE with no accession is clean — while the invariant names "a NAME or
  accession with a structure". My §5 claim that "the format most likely to carry a whole record"
  is now visible must be NARROWED to "visible for the accession half".
Q6-16 [MED] The guard vocabulary (~140 lines, five path-policy sets) now outweighs anything
  snapshot-shaped in drugbank_snapshot.py and wants its own module; and BINARY_ALLOWLIST answers a
  COVERAGE question while its neighbours answer a CONTENT-PERMISSION question, with nothing making
  the distinction visible. Both are moves I should make with a clause, not without one.
Q6-17 [MED] THE FAIL LIVES IN A TEST, not in the API: `real_accession_hits` alone is still the
  false-clean, and the pairing is asserted only in a docstring. The day this is wired into CI the
  natural call is the single function. A combined entry point is the fix — and is a shape decision.

PROCESS:
Q6-18 [GOOD] The throwaway copy WORKED. Four reviewers, heavy mutation, zero contamination of the
  hashed tree — after three occurrences today. The structural fix beat three rounds of instruction.
Q6-19 [AGAINST ME] I wrote the §6 implementation BEFORE its tests again. I recovered the evidence
  by running the new tests against the pre-fix module in the copy (7 fail), which is real evidence
  — but it is evidence gathered after the fact, and the reviewers' own findings are what told me
  where to look. Red-first would have cost nothing here.
