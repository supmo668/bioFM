# §4 (G-19 + ruff format + quality config) — findings. Base b801329, Hash A b3d8a2e.

Scope: 3 commits — d0077a2 (read_pgp_label_frame carries the flag), f878a26 (ruff format, 5
pre-existing files), 2e81e97 (agency.yaml quality.format_command + lint_command).

REDUCED REVIEWER ROSTER, disclosed: 2 reviewers (code, design) instead of the canonical 4, plus my
own review. Reason: ~20 lines of new code, one formatting-only commit, one config line. The
security and test dimensions were covered directly — the record-content guard suite runs in the
checks below, and the two new tests were mutation-verified. Flagged in the receipt, not silently
narrowed.

## own review

OWN-1 [VERIFIED CLEAN, method correction] "Formatting only" (f878a26) checked by TOKEN STREAM
  comparison against b801329, not by reading the diff:
    ids.py 3108 tokens IDENTICAL · merge_report.py 1224 IDENTICAL · drugbank_snapshot.py 2665
    IDENTICAL (this is the file holding the record-content guard regexes) ·
    test_merge_report_record_content.py 568 IDENTICAL ·
    test_parse.py 1839 -> 1837: the ONLY difference is a removed redundant parenthesis pair around
    an unchanged string literal (MALATE_DIANION_TAUTOMER). The literal's bytes are identical.
  So the claim holds. Recording the two-token delta rather than rounding it to "identical": my
  checker counts parentheses, and a reader should see why the number moved.

OWN-2 [MEDIUM, own, PROCESS] I wrote the G-19 code and its tests in the SAME step, so the tests
  passed on their first run and were never seen red. I recovered the evidence by mutating the
  reader back to the old dropping-the-flag behaviour (and separately relaxing the missing-column
  check): both mutations KILLED, so the tests do discriminate. But red-then-green was not
  observed, and mutation-after-the-fact is a weaker guarantee than a test seen failing for the
  stated reason. Recorded against myself.

OWN-3 [LOW] `read_pgp_label_frame` coerces `stereo_is_relative` with `.astype(bool)` on a column
  read back from parquet. Unlike the compounds path (QG G-03), coercion here is safe because the
  writer pins the dtype and the reader refuses the column's absence — but the two paths now treat
  the same column name by different rules (refuse vs coerce), which a later reader could
  mis-generalise. Deliberate; noted so it is a decision, not an inconsistency.

OWN-4 [MEDIUM -> CTO] agency.yaml documents `quality.lint_command_<agent>` as an existing
  convention (in the referee_command comment block), but I could find per-module resolution
  implemented ONLY in tools/referee (`tests.referee_command_<module>`). The quality gate reads the
  plain `quality.lint_command`, so a per-agent quality override would silently do nothing. That
  makes the comment misleading and the global key the only working lever — which is why the
  principal-approved change had to be global. Framework-level; reported, not fixed here.

## reviewer-design (config blast radius) — VERDICT: my global setting was the wrong lever

DES-1 [HIGH, CONFIRMED BY ME, FIXED 6011dec] My OWN-4 above was BACKWARDS. hooks/quality-check.sh
  — the blocking Stop hook — resolves `<key>_<agent>` FIRST (line 70) and, when project.modules is
  non-empty, RETURNS EARLY rather than falling back to the global (line 75). I verified both lines
  and `project.modules` = ['perturb-seq-eval','cellforge-agents','lung-on-chipsim']. So the global
  keys I set were unreachable from the blocking gate; the per-agent key is the only lever. My
  earlier grep covered tools/ and missed hooks/ — an absence-of-evidence conclusion from an
  incomplete search, stated as fact in a committed comment.
DES-2 [HIGH, CONFIRMED BY ME, FIXED 6011dec] The command was UNANCHORED. Both mechanical consumers
  cd to the git toplevel = the WORKTREE ROOT, where there is no pyproject.toml. Verified by
  running it there: `uv run ruff check .` -> "error: Failed to spawn: `ruff`". So the check I
  "enabled" could not have passed anywhere it actually runs. Now `cd projects/lung-on-chipsim &&
  ...`, verified exit 0 from the worktree root for both commands.
DES-3 [HIGH, FIXED 6011dec] Blast radius on the three sibling agents: cellforge-agents
  (setuptools, ruff only in optional-dependencies, which uv does not install), perturb-seq-eval
  (Poetry — uv reads neither its dev group nor its lock, and would WRITE a stray uv.lock into a
  Poetry project), aviary-biosim (a submodule worktree with no Python packaging at all, which
  nevertheless resolves this same agency.yaml). All three would have carried a permanently red
  check. They are now unset and legitimately skip.
DES-4 [MED, reported to CTO] tools/commit-precheck runs format/lint NON-BLOCKING and discards
  output, so between 2e81e97 and 6011dec every git-safe-commit ran a command that exited 2 and
  reported nothing — worse than the honest "not configured — skipping" it replaced. Three commits
  (d0077a2, f878a26, 2e81e97 itself) were made in that window; the checks they silently failed are
  the ones I ran by hand anyway and recorded here.
DES-5 [MED, reported] commit-precheck reads only the PLAIN key (no per-agent resolution), so with
  the globals empty it goes back to skipping, while quality-check.sh now enforces per-agent. An
  upstream inconsistency between the two consumers — worth aiadlc feedback, not a local fix.
DES-6 [LOW, latent] cicd.gates declares lint/typecheck for a CI pipeline that does not exist yet
  (no .github/). Whoever authors it will read the plain key and inherit the anchor problem.
DES-7 [INFO, aiadlc feedback] The upstream schema doc (config/agency.schema.md:38) carries the
  same inverted claim and never documents the `quality.*_command_<agent>` rows that
  hooks/quality-check.sh actually implements. That undocumented gap is what produced my error.

## reviewer-code (b878a26 formatting verified CLEAN independently; read_pgp_label_frame NOT clean)

COD-1 [HIGH, FIXED 70a0aae] `read_pgp_label_frame` coerced the flag with `.astype(bool)`,
  re-introducing G-03 ONE FILE DOWNSTREAM, on the file T17 reads. Measured by the reviewer against
  the installed pandas: float64 with NaN -> [True, True]; strings "false"/"False" -> [True, True]
  (the CSV-round-trip case this module names explicitly); object with None -> [False, False],
  silently UNflagging; nullable boolean with pd.NA -> a bare pandas ValueError, leaking the error
  contract. Clean bool dtype returned in every case, so no caller could tell fabricated flags from
  real ones. My OWN-3 had called this coercion "safe because the writer pins the dtype" — wrong:
  the reader's job is to distrust the file. Now refused, same rule as _relative_by_key.
COD-2 [MED, FIXED 70a0aae] `frame.index.name = "canonical_inchikey"` was assigned unconditionally,
  manufacturing the guarantee it appeared to check: a RangeIndex came back as integers labelled
  `canonical_inchikey`; an index named `wrong_key` was silently relabelled. A T15->T17 join then
  matches nothing or the wrong rows, with no error. Now verified; only an UNNAMED index is labelled.
COD-3 [MED, FIXED 70a0aae] Duplicate keys accepted by the reader while every WRITE path rejects
  them; `.loc[key]` then returns a Series and the consumer idiom dies on an ambiguous truth value
  — an idiom my own new test uses. Now rejected.
COD-4 [LOW, FIXED 70a0aae] The missing-column refusal blamed the relative-stereo re-key whatever
  was missing, misdiagnosing a file that was never a label set. Now branched.
COD-5 [LOW, FIXED 70a0aae] Extra columns silently dropped vs the tracked reader's refusal. Kept
  lenient deliberately (this parquet is ours to write; a future column is additive) and now
  documented in the code AND pinned by a test, so `return frame` cannot slip through.
COD-6 [INFO, FIXED 70a0aae] The index-name fixup in `read_pgp_labels` was dead once the frame
  reader guarantees it — found by a mutation that deleted it and passed 47/47.
COD-7 [HIGH] = the agency.yaml global. Independently measured: `ruff check .` from the repo root
  gives 257 errors and `ruff format --check .` 112 files; perturb-seq-eval 71 files;
  cellforge-agents 7; aviary-biosim has no pyproject at all. Already FIXED in 6011dec (per-agent
  anchored keys) before this report arrived; the reviewer worked from the pre-fix state.
COD-8 [INFO, disclosed] My f878a26 commit message says "61 files already formatted"; the true
  count is 67. A wrong number inside a receipted claim. Cannot rewrite a pushed-parent commit
  message; corrected here and in the receipt.

FIVE MUTANTS the reviewer found SURVIVING my G-19 tests (dtype cast, index-name assignment,
extra-column drop, adjudicated_label guard, dead line) are each KILLED by the tests added in
70a0aae — verified by re-running them, restored byte-identical afterwards.

## Gate result
Fixed here: COD-1..COD-6, DES-1..DES-3 (config). Disclosed: COD-8, DES-4, DES-5 (framework-level).
Escalated to CTO / aiadlc feedback: DES-5, DES-6, DES-7, and the commit-precheck silent-failure
window (DES-4).
Checks, fresh on the committed state: 711 passed / 5 skipped / 0 failed; `ruff check` clean;
`ruff format --check` clean (67 files). Both run through the NEWLY CONFIGURED per-agent commands,
from the worktree root, exactly as the hook will run them.
