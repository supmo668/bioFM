# QGR consolidated findings — lung-on-chipsim re-gate (r2.7 work order)
Roster: reviewer-code, reviewer-security, reviewer-design, reviewer-test + own review.
Threshold: 80. Findings below are post-scorer survivors, deduplicated.

## Own review (pre-roster), all reproduced by execution
SELF-1 Malformed configs/env.yaml carrying `seed: 42` recorded source:"unset" — positive false statement. 95
SELF-2 TOCTOU between escape check and copy. 85 (first fix inadequate; see SEC-2)
SELF-3 argv[0] ending in separator became "" — total loss of what ran. 85
SELF-4 int("1_0")==10 — recorded seed differs from what a human reads. 85
SELF-5 Symlink DIAMOND enumerated exponentially: 12 levels -> 8,178 files in 5.0s. 95

## Security (reviewer-security)
SEC-1 HARD LINK bypasses the escape control entirely; resolve() cannot see it. Reproduced leaking TOPSECRET. 95
SEC-2 TOCTOU still open after SELF-2: copy2(verified) re-opens BY PATH. Comment asserted a property the code lacked. 90
SEC-3 UNRECORDED-*.json marker writes raw sys.argv, defeating B2 on the path that fires when journalling fails. 97
SEC-4 _resolve_seed reads configs/env.yaml with no escape check; record_invocation (panel-seal) unprotected. 88
SEC-5 Config escape did not stop the run — stage executed, process exited 0. 85
SEC-6 _verify_config_snapshot uses rglob — same bug as the write path; planted files invisible, tampered record verifies clean. 85
SEC-8 _coerce_seed echoed rejected config values into durable markers. 78

## Code correctness (reviewer-code)
CODE-2 Exponential walk (independent confirmation of SELF-5): n=16 -> 65,535 paths / 90.7s. 95
CODE-4 Exported-but-empty CHIPSIM_SEED treated as present-invalid; bricks panel-seal and de-journals every ETL run. 90
CODE-5 An escaping linked dir with no YAML under it hard-fails start_run. 88
CODE-10 iterdir() leaks raw PermissionError past callers catching JournalError. 90
CODE-11 Broken symlink named *.yaml silently skipped — is_dir/is_file both False. 85
CODE-14 environment.seeds shape change unversioned. 90

## Design (reviewer-design)
DES-1 "shows the file is unmodified" overclaims an unkeyed digest; repo's sanctioned verb is "detects a later edit". 88
DES-2 argparse description still told the human AT SEAL TIME that C4 has "no technical enforcement", and never mentioned the TTY requirement. 88
DES-3 "FIRST technical enforcement" contradicts three surviving "the only mechanical support" claims. 82
DES-4 test_replay_determinism.py self-named as "The PoC form of the replay test" while its body says that form is not executable. 80
DES-5 _config_sources docstring attributed the directory refusal to "the caller's check"; it is done in the walk. 90
DES-7 A&D §4.4a-ii still asserted "a dirty tree is recorded, never hidden or refused" after ADR-0001 deleted git capture. 82
DES-8 A&D table row restated record contents, omitting five §4.4a minimums. 72
DES-15 subparser help said T8; everything else says T7a. 80

## Test quality (reviewer-test)
TEST-1 No coverage for the hard-link route (== SEC-1). 95
TEST-2 TTY tests journalled into the REAL project tree — 88 agent-authored records in Constraint (4)'s audit trail. 98
TEST-3 bool seed guard had ZERO coverage; deleting it left all tests green while `seed: true` recorded as 1. 97
TEST-4 Four seed tests fail under an exported CHIPSIM_SEED. 97
TEST-5 TTY honesty grep was theatre — a one-word paraphrase slipped all five literals. 93
TEST-6 A seed in configs/env.yml recorded as false "unset". 90
TEST-8 test_panel_seal_proceeds_past_the_gate: "ratified_panel_sha256 in text" true on the untouched fixture — vacuous. 95
TEST-9 test_panel_seal_writes_nothing had teeth only by accident of filename-binding. 88
TEST-11 Determinism pair ran in one interpreter — cannot fail for hash-seed nondeterminism. 88
TEST-12 min_rows=0 lets an empty frame pass the determinism control. 85
TEST-16 Test docstring asserted "rglob walks into it"; rglob does not. 96
