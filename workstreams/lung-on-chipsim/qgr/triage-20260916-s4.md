# §4 (G-19 + ruff format + quality config) — TRIAGE. Base b801329, Hash A b3d8a2e. Threshold 80.

Roster: 2 reviewers (code, design) + own review, instead of the canonical 4 — disclosed in the
receipt. ~20 lines of new code, one formatting-only commit, one config line. Security and test
dimensions were covered directly: the record-content guard suite runs in the checks, and every new
test was mutation-verified rather than assumed.

ACCEPT — fixed in this gate:
QG4-01 [96] COD-1 the label-parquet reader COERCED the flag, re-introducing G-03 one file
  downstream on the file T17 reads (strings/NaN -> True for every row, clean dtype, no error).
  Refused now, the same rule as the compounds path. This is the finding that justified the gate.
QG4-02 [90] COD-2 the index name was assigned, not verified — manufacturing the guarantee and
  letting a downstream join match nothing. Verified now; only an unnamed index is labelled.
QG4-03 [88] COD-3 duplicate keys accepted by the reader while every write path rejects them.
QG4-04 [86] DES-1 my agency.yaml comment was INVERTED: the blocking hook resolves per-agent FIRST
  and skips the global when project.modules is non-empty. I verified both lines myself before
  acting. Fixed in 6011dec.
QG4-05 [86] DES-2 the command was unanchored and could not pass anywhere it actually runs
  ("Failed to spawn: ruff" from the worktree root). Anchored with cd; both verified exit 0.
QG4-06 [85] DES-3 the global would have handed three sibling agents a permanently red check
  (setuptools / Poetry / no packaging at all). They are unset and legitimately skip.
QG4-07 [82] COD-4 the missing-column refusal misdiagnosed a file that was never a label set.
QG4-08 [80] COD-5 extra columns dropped silently — kept deliberate, now documented AND pinned.
QG4-09 [80] COD-6 dead index-name line in read_pgp_labels (found by a surviving mutant).

DISCLOSED, not fixable here:
QG4-10 COD-8 my f878a26 commit message says "61 files already formatted"; the true count is 67.
  The commit is already pushed's parent — I will not rewrite history to fix a message. Corrected
  in the findings file and in this receipt.
QG4-11 DES-4 between 2e81e97 and 6011dec, tools/commit-precheck ran format/lint NON-BLOCKING with
  output discarded, so three commits passed a permanently-red, invisible check. Worse than the
  honest "not configured — skipping" it replaced. The checks were run by hand and recorded.

ESCALATED (framework-level, aiadlc feedback — not this repo's to fix):
QG4-12 DES-5 tools/commit-precheck reads only the PLAIN key while hooks/quality-check.sh resolves
  per-agent: with the globals correctly empty, precheck skips while the hook enforces.
QG4-13 DES-7 the upstream schema doc carries the same inverted claim and never documents the
  `quality.*_command_<agent>` rows the hook implements. That undocumented gap produced my error.
QG4-14 DES-6 cicd.gates declares lint/typecheck for a CI pipeline that does not exist yet; whoever
  authors it will read the plain key and inherit the anchoring problem.

PROCESS (recorded against myself):
OWN-2 I wrote the G-19 code and tests in the SAME step, so the tests were never seen red. I
  recovered the evidence by mutation afterwards and they do discriminate — but the reviewer then
  found FIVE more mutants surviving those same tests, which is precisely what a red-first test
  would have made me confront earlier. Red-then-green is not ceremony; skipping it cost a round.
OWN-1 "Formatting only" was verified by token-stream comparison, and the reviewer independently
  confirmed it by AST comparison including comment tokens. Both found the same single difference:
  a redundant parenthesis pair around an unchanged literal.
