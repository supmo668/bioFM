# QGR triage — disposition per finding
Policy: fix everything, defer nothing. Every finding below is ACCEPTED and FIXED.
Each fix carries a regression test that fails without it, except where noted.

ACCEPTED + FIXED — security
SEC-1 hard link  -> _open_verified_config: in-root + st_nlink==1 + S_ISREG, O_NOFOLLOW, copy from fd. Test: test_a_hard_linked_config_is_refused.
SEC-2 TOCTOU     -> read from the descriptor; never re-open by path. Residual window DOCUMENTED as narrowed, not closed — the previous comment overclaimed and that is why it recurred.
SEC-3 marker argv-> marker uses _recorded_argv.
SEC-4 seed read  -> escape check moved onto the READ, so record_invocation is covered too.
SEC-5 exit 0     -> new ConfigIntegrityError exempt from best-effort; main returns 2 without running the stage.
SEC-6 read rglob -> explicit scan; symlinks under a snapshot dir refused outright. Test: test_a_file_planted_under_a_symlinked_snapshot_dir_is_detected.
SEC-8 value leak -> messages name type/origin, never the value.

ACCEPTED + FIXED — correctness
SELF-1..5, CODE-2/4/5/10/11/14 -> all fixed; bounded walk (512 dirs / depth 32) raises rather than truncating, preserving the alias-drop fix.
CODE-5 note: an escaping linked dir is still refused even with no YAML beneath it. KEPT deliberately — the dir is an escape regardless of current contents, and descending first to decide would mean walking outside the root to find out.

ACCEPTED + FIXED — design/honesty
DES-1/2/3/4/5/7/8/15 -> all fixed. Seal limit uses the sanctioned verb; argparse description states the TTY requirement; A&D contradictions struck.

ACCEPTED + FIXED — tests
TEST-1/2/3/4/5/6/8/9/11/12/16 -> all fixed. 88 test-authored records cleared from the live C4 trail (all verified test-authored; trail gitignored, only .gitkeep tracked). Suite verified to add ZERO records after the fix.
TEST-5 note: the weak literal blocklist was DELETED rather than extended, and the negative check consolidated into test_journal.py's regex guard (negation window + anti-vacuity). Mutation-verified: the paraphrase that defeated the old guard now fails the suite.

DEFERRED — none.

ESCALATED TO THE CTO (not fixable at this layer, no silent workaround applied)
E-1 "SCORES DO NOT EXIST." The PoC replay form is specified over scores; heads/ and
    uncertainty/ are empty and no build-plan task produces one. The literal item (3)
    is not executable. Delivered the strongest true form (byte-identical persisted
    artifact, cross-process) and labelled it as a STAND-IN, not the PoC form.
    Nothing claims the PoC replay test is closed.
E-2 plan-approval.md was rewritten at 02:08 mid-session, stripping all five B4
    disclosure fields and the provenance block. plan-gate verify PASSED before and
    after — the hash cannot see it. Restored; the removal had been swept into my own
    commit 26b6780 by a blanket `git add -A`, which is a process defect on my side.
E-3 SEC-7 (reviewer-security, conf 80): the boundary is the project root, and the
    root also holds .env and .venv. `configs/x.yaml -> ../.env` resolves IN-root and
    is copied into the published record. This FOLLOWS the ruling as written, so it is
    not an implementation defect — but it is a policy question for the principal:
    restrict to real files whose real parent is configs/, or state that in-root
    secrets are in scope for publication. NOT silently changed.
E-4 SEC-9: the TTY gate tests for a terminal but never READS from it, so any process
    inheriting a terminal passes it unattended — a cheaper bypass than pty allocation
    and not currently stated on the model card. Recommend a confirmation read; not
    added unilaterally because it changes the human's sealing UX.
