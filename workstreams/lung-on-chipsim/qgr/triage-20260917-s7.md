# §7 (r2.20 allow-list + r2.21 E6-2/E6-4/E6-5 + r2.22 E6-1b) — TRIAGE.
Base 1a5d72f, Hash A f520f12. FULL roster on a throwaway copy; all four confirmed the real tree
ended byte-identical. Threshold 80.

THE PATTERN, stated once because it held for nearly every finding: each defect was the lesson of
the clause directly above it, reappearing one level down in my implementation of that clause.

FIXED — Phase A, the four EXECUTED write-path bypasses (7924cd0):
A-01 [98] THE `.tmp` SIBLING WAS NEVER CHECKED. The guard validated `out`; the writer wrote
  `out.name + ".tmp"`, a predictable name, and `to_csv` follows a symlink. A fully-allowed call
  put the name-bearing worksheet in the tracked directory — bypass #3 from the r2.19 post-mortem,
  moved one filename over, inside the guard written to close it. Now mkstemp (O_EXCL, random name).
A-02 [96] HARD LINKS were not refused, only symlinks: a linked tracked file was overwritten IN
  PLACE with accession + name + InChI + InChIKey on one row.
A-03 [95] THE TMP ROOT CAME FROM $TMPDIR — an env-var-redirectable sink, so pointing it at the
  project made the ENTIRE TRACKED TREE writable. `journal.source_root()`'s own docstring refuses
  exactly this, and the CTO had flagged this defect family (four in two days, all "identity from
  ambient shell state") ONE HOUR before I wrote it. Now test-only and refused when it overlaps.
A-04 [92] THE "UNTRACKED" ROOTS CONTAIN TRACKED NAMES: .gitignore re-includes `*.dvc`, `.gitkeep`
  and `data/processed/*.sha256`, so writing the worksheet to a `.sha256` name there was accepted —
  untracked BY DIRECTORY, tracked BY SUFFIX.

FIXED — Phase B, "always READ" was false for every real container (ca273dc):
B-01 [97] `repr(array)` ELIDES above 1,000 elements — the identical defect `_parquet_chunks` fixed
  and pinned one clause earlier, reintroduced in the reader written to close the container gap.
  The repo's own 34.6 MB container scanned to 4,270 chars (six of ~41,000 identifiers), and the
  0.01s I cited as evidence the cost was fine was a measurement OF the elision. Now 1,212,890
  chars, zero elision markers, 0.03s.
B-02 [94] COMPOUND dtypes skipped — the on-disk shape of every HDF5 table and of legacy AnnData
  obs/var. A dataset holding the whole record scanned to SEVEN characters.
B-03 [90] An unreadable dataset was annotated `<unreadable>` and the file still counted as READ and
  CLEAN. B-04 [88] A link-only container yielded an EMPTY chunk and passed as "read".
B-05 [86] A parquet struct column stringified its KEYS. B-06 [85] No per-dataset memory bound
  (206 KB compressed -> 200 MB, 950x). B-07 [84] "No reader" degraded to "declare it", the one
  answer E6-2 forbids for a container; now a distinct propagating error.

FIXED — Phase C, the registry and the vacuous assertions (4de0b33):
C-01 [96] THE REGISTRY SAW TWO METHOD NAMES. `pq.write_table`, `to_feather`, `to_hdf` and bare
  `open(..., "w")` all opted out silently — a CLI persisting the complete record to any tracked
  path via `pq.write_table` SURVIVED THE FULL SUITE. Widening it surfaced SIX invisible writers.
C-02 [95] A PHANTOM DECLARATION: `merge_report.write_merge_report` does not exist, and
  `found <= declared` let it pass while the real writer stayed invisible. Now `declared <= found`.
C-03 [94] MY SYMLINK TEST PASSED ON ITS OWN DIRECTORY NAME — pytest names tmp_path after the test,
  so `match="symlink"` matched the path with the destination check DELETED. Verbatim the trap the
  r2.19 test I deleted had documented, reintroduced by my own migration.
C-04 [92] OWNERSHIP INVENTED PROJECTS: `projects/README.pdf` -> owner "README.pdf", failing
  nobody's gate — the double-exempt hole E6-4 closed, one function below the comment calling
  unowned-fails-here LOAD-BEARING FOR E6-4. An owner must now own a SUBTREE.
C-05 [88] The CLI exited with a RAW TRACEBACK on a refused destination while MY OWN TEST asserted
  in prose that the handler existed. C-06 [85] `THIS_PROJECT` was a bare literal: renaming the
  project directory would turn every owned path into somebody else's and the gate would go GREEN.
C-07 [84] The guards package docstring claimed the read half it does not hold — an overclaim by a
  guard one directory above the docstrings E6-5 made scrupulous about exactly that.

FIXED — Phase D, messagehood, naming, and the unread report (4b28055):
D-01 [96] A BINARY NAMED `.md` WAS DOUBLE-EXEMPT. Messagehood was decided by suffix; a message is
  TEXT. Now the waiver requires the payload to decode.
D-02 [88] Declarations renamed to RENDERED_ARTIFACT_DECLARATIONS (E6-2's "named distinctly" is the
  whole requirement, and an empty list satisfied no naming rule), with a test that every
  declaration is OWNED here — E6-1's actual invariant, which nothing tested.
D-03 [86] The container check dispatched on SUFFIX — name-based dispatch again; now MAGIC.
D-04 [85] THE REPORT REACHED NOBODY. `chipsim record-content-report` now prints it (23 listed with
  owners, 0 failing, exit 0). The CTO asked this at the §6 boundary: "listing that reaches no one
  is functionally a silent skip".

ESCALATED — not mine to decide:
E-01 [HIGH] TWO MORE RECORD-BEARING WRITERS, invisible until the registry was widened:
  `fetch_snapshot` (writes the raw DrugBank tables to an unvalidated --dest: the most
  record-bearing payload in the project) and `merge_report.main` (operator-chosen --out; "a tracked
  merge report came to carry 89 real accessions" is the incident this guard's docstrings cite).
  Neither legitimate destination — data/raw/, the journal — is a declared root, so r2.20's helper
  cannot be applied as written. Recorded in RECORD_BEARING_PENDING_RULING so the registry STATES
  the gap rather than hiding it behind a comfortable classification.
E-02 [MED] E6-1's per-project declaration DATA (path -> sha256, or "derived from tracked source S")
  is still unbuilt: nothing reads a union, and the sha256 content-pinning E6-3 folded in does not
  exist. I delivered only the removal half, and E6-1b's scoping keeps the suite green without it —
  which is exactly why it is easy to miss.
E-03 [MED] "Fails their owner's gate" is FICTIONAL today: no other project implements this gate, so
  the 23 listed files fail nowhere. My comment asserted otherwise.
E-04 [MED] r2.20 says containment "case-insensitively"; I used directory IDENTITY instead, because
  for an ALLOW-list case-folding is the PERMISSIVE direction. Substantively safer in every case
  where they differ — but it replaces a mechanism a SIGNED clause names, recorded only in a
  docstring. Wants a one-line amendment; the code should not change.
E-05 [MED] Unowned = every repo-root location (docs/, config/, research/, tools/, .claude/), so a
  new `docs/architecture.png` from anyone fails THIS gate, and the only remedy is a declaration my
  own comment forbids. E-06 [LOW] Declared roots must EXIST or legitimate writes are refused with a
  message naming that root as allowed; masked today by .gitkeep files nothing links to the guard.
E-07 [LOW] Anchoring to the installed package tree means a non-editable install refuses every
  record-bearing write.

PROCESS:
P-01 [GOOD] The throwaway copy worked a second time: four reviewers, heavy mutation, zero
  contamination. One reviewer found the copy destroyed mid-session by a concurrent reviewer and
  built its own — worth a per-reviewer path next time.
P-02 [AGAINST ME] Two reviewers independently caught assertions that pass on the TEST'S OWN NAME.
  That is now three times in one session (tmp_path "already", "configs", "symlink"). The lesson is
  not "be careful with match=" but: never assert on a string the fixture also produces.
P-03 [AGAINST ME] Four of my new tests passed on arrival — behaviour already correct, merely
  untested. Recorded as coverage pins, not claimed as fixes.
