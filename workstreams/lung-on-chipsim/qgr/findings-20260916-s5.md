# §5 (r2.19 G-15 path refusal + G-18 CLI) — findings. Base 83188df, Hash A 8330478.

Roster: FULL (code, security, design, test) per the CTO's standing rule — this diff touches an
interface (a new CLI subcommand) and the human artifact's write path.

## own review

OWN-1 [MEDIUM, own] `pytest.raises(AdjudicationError, match="configs")` in the two G-15 tests is
  NOT discriminating. The tests write to `tmp_path / "configs" / ...`, so the PATH interpolated
  into any error message contains "configs". Measured: an unrelated `AdjudicationError(f"some
  unrelated failure at {target}")` also satisfies the match. This is the same trap as the earlier
  tmp_path-named-after-the-test vacuity, one layer along — I flagged it to the test reviewer and
  then confirmed it myself rather than waiting. Fix: match a phrase that only the refusal can
  produce ("refusing to write a worksheet"), and assert the remedy sentence ("only writer
  permitted") is present.
  EDIT HELD until the reviewers hand back: they are reading these files, and editing the tree
  mid-review is what produced a false HIGH finding in the §3 gate. The discipline I asked of them
  applies to me.

OWN-2 [LOW, own, to confirm] `TRACKED_DIR_NAME in Path(out).resolve().parts` matches a directory
  named `configs` ANYWHERE in the resolved path, including outside the repo entirely. For this
  codebase that is the safe direction (the only legitimate worksheet destination is
  data/interim/), but it means a caller with an unrelated `~/configs/` scratch directory is
  refused. Asked the code and design reviewers to judge over- vs under-reach rather than assuming
  my own reading is right.

OWN-3 [LOW, own] The G-15 refusal LIVES in T13's function but is DOCUMENTED only in T14's plan
  block. A reader of T13 will not find the rule that governs T13's own writer. Raised with the
  design reviewer as part of the composition check; if confirmed it is a plan note for the CTO,
  not a code change.

OWN-4 [PROCESS, HIGH — RECURRENCE] A reviewer is mutating the SHARED WORKING TREE again, mid-gate,
  despite the explicit protocol in every prompt this round ("save bytes, mutate, run, restore in
  the SAME command, or work on a copy"). Observed live:
    - pipeline.py: the design reviewer saw `_cmd_adjudication_export` returning 1 instead of 2;
      by the time I checked, `git diff` on that file was EMPTY (restored).
    - adjudication.py: `git diff` shows `.resolve()` REMOVED from the G-15 check RIGHT NOW,
      while two reviewers are still running.
  This is the third occurrence today (it produced the rejected CODE-1 in §3). The gate's own
  integrity depends on the tree being what the receipt hashes: Hash A was captured from the
  COMMITTED state, so it is unaffected, and I will verify byte-identity against HEAD before
  capturing Hash E or committing anything. But a reviewer that crashes mid-mutation leaves a
  defect in the tree that the next hash would attest to. RULE FOR ME: never hash, never commit,
  never push while a reviewer is live — verify `git diff` is empty first, every time.

## reviewer-code + reviewer-design (G-15 predicate is WRONG in three directions — I reproduced all)

G5-01 [HIGH, confirmed by me in a temp tree] CASE BYPASS. `configs/` refused, `CONFIGS/` ALLOWED —
  and on this case-insensitive volume the write LANDED IN THE REAL `configs/` DIRECTORY (verified:
  the real dir then held w.csv). One shifted keystroke defeats the guard, and git would show the
  result as `configs/pgp_adjudication.csv`, committable, carrying `name` at position 2.
G5-02 [MED, confirmed] SYMLINK. `sym/configs -> tracked`: ALLOWED. `.resolve()` strips the
  `configs` component before the check sees it, so resolving — which I documented as the
  STRENGTHENING — is what defeats this case. journal.py already treats a `configs/` that resolves
  elsewhere as a first-class hazard.
G5-03 [MED, confirmed] OVER-BREADTH. A checkout under `<anything>/configs/` refuses EVERY
  worksheet write, including the legitimate data/interim/ one: `~/configs/checkout/data/interim/
  w.csv` -> refused. T13 stops working entirely, with a message telling the reviewer to write
  where they already are.
G5-04 [LOW, confirmed] A symlink loop raises RuntimeError, not AdjudicationError, so it escapes
  every `except AdjudicationError` — including the new CLI handler's, producing the traceback that
  handler exists to prevent.
G5-05 [MED-HIGH] E-3 CONVENTION BREACH (design + code agree): journal.py:557 `_configs_boundary`
  declares itself "the ONE boundary every reader of `configs/` must use", and hardens exactly the
  hazards above (anchored to the project root; refuses a configs/ that resolves outside). My guard
  invented a second, weaker definition in another module — the condition journal.py's own docstring
  warns produces an unmaintained boundary.
G5-06 [MED] CLI catches only AdjudicationError, so the likeliest operator error at the end of a
  60-90 minute task — one transposed letter in --worksheet — is a raw traceback
  (FileNotFoundError / EmptyDataError / IsADirectoryError all measured).
G5-07 [MED] MY CLI TESTS POLLUTE THE REAL AUDIT TRAIL. They call pipeline.main without
  redirecting CHIPSIM_PROJECT_ROOT, and the command is NON_ETL, so every pytest run writes genuine
  invocation records into projects/lung-on-chipsim/journal/invocations/ — 4 before review, 32
  after the reviewer's runs. test_panel_seal_tty.py documents this exact mistake and its fix; I
  repeated it. Second-order: the tests silently require a writable source tree.
G5-08 [MED] MY TEST DOES NOT TEST ITS OWN NAME. `test_the_configs_refusal_resolves_the_path_...`:
  removing `.resolve()` entirely leaves the WHOLE SUITE GREEN, because the test's input contains
  the literal component anyway. Discriminating input: chdir into the configs dir and pass a bare
  relative filename.
G5-09 [LOW] `assert not target.exists()` does not cover directory creation: adding an mkdir before
  the raise leaves all 48 file tests green.
G5-10 [MED-HIGH] FAIL-CLOSED BY INHERITANCE (design). `NON_ETL_SUBCOMMANDS` is the workflow
  partition a test requires, and main() reuses that same tuple to apply panel-seal's fail-closed
  journalling. So the policy was inherited by naming coincidence; no r2.19 clause states it.
  panel-seal fails closed because an unrecorded seal is the thing its trail exists to expose; an
  export is idempotent, never clobbers, and is reconstructible from the worksheet.
G5-11 [HIGH, design] FALSE RECOVERY INSTRUCTION (pre-existing text, sealed shut by G-15): the G-01
  refusal says "write_adjudication_worksheet merges the tracked verdicts back in". It does not —
  it merges only from a worksheet already at `out`, which is precisely what is missing in the
  scenario the message describes. G-15 now refuses the configs/ workaround, and every other
  message forbids hand-editing. No recovery path exists in the codebase.
G5-12 [MED, design] T14's Files clause still says "move to configs/" — the hand-move its own r2.19
  block calls the accident. Plan-level, CTO's.
G5-13 [MED, design] T14's Done-when (every row has a verdict) is enforced nowhere at export time:
  a first export of a wholly-blank worksheet prints "wrote 24 row(s)" — success language at the
  moment the human believes they are done.
G5-14 [MED, design] The G-15 refusal is undiscoverable from T13: absent from T13's plan interface,
  T13's Done-when, AND the public docstring of the function it guards.
G5-15 [MED, design] The code docstring still says "a wholly unadjudicated WORKSHEET" — the exact
  wording r2.19 repaired in the plan, still live one layer down.
G5-16 [MED, design] TRACKED_DIR_NAME is public, generically named, and its comment claims "only
  export_tracked_adjudication may write here" — false: seal_panel writes configs/barrier_panel.yaml.

## reviewer-test (21 mutants; TDD claim VERIFIED red-first; tree restored byte-identical)
G5-17 [CRITICAL] The `match="configs"` trap, proven by a mutant pair I could not argue with:
  a refusal message reduced to the BARE PATH survives, an UNRELATED "permission denied" message
  echoing the path survives, and the CORRECT explanation WITHOUT the path interpolated is KILLED.
  The assertion binds to the tmp_path string and actively rejects a correct message. (I found the
  same trap independently before this report; the mutant pair is the proof I did not have.)
G5-18 [CRITICAL] The resolution test: BOTH a no-resolve implementation and a naive
  `"configs" in str(out)` substring implementation survive the entire suite. The one property the
  test is named for is unverified. Only a chdir-relative destination distinguishes them.
G5-19 [HIGH] A substring implementation also survives the narrowness test (over-refusal untested):
  `myconfigs_backup/` and `my_configs_archive/` are correctly allowed by the real code, asserted
  nowhere.
G5-20 [HIGH] A `parent.name == "configs"` implementation survives: every refusal test puts the
  file DIRECTLY in configs/, so "anything UNDER configs/" — configs/sub/deep/w.csv — is untested.
G5-21 [HIGH] = G5-02 symlink, with the write measured: the worksheet landed in the link target
  with `name` included.
G5-22 [HIGH] = G5-07 journal pollution, measured precisely: "real +2, scratch +0" without the
  override, "real +0, scratch +2" with it. 34 records now in the real trail (4 mine, ~30 the
  reviewer's). Gitignored, so nothing is committable — but NOT pristine, and the reviewer rightly
  refused to delete audit-trail files on its own authority. Disclosed in the receipt.
G5-23 [HIGH] = G5-06, with the measurement: a typo'd --worksheet gives a 25-line pandas traceback
  and exit 1 — the exact failure mode the command's own docstring claims to abolish.
G5-24 [MED] The refusal exit code is unpinned (`!= 0`): returning 1 survives, while panel-seal is
  pinned to exactly 2 in two places.
G5-25 [MED] The CLI success path asserts nothing the operator sees: deleting the "wrote N row(s)"
  line survives, and so does printing rows+1 — a WRONG COUNT reported as fact.
G5-26 [MED] The fail-closed journalling consequence is untested for this command: that an
  unjournallable invocation does not publish the tracked file.
G5-27 [LOW] The escape test does not assert that nothing was written.
CLEAN per the reviewer: --out wiring, refusal-to-stdout, refusal detail ("notes" does real work),
  the writer/export ASYMMETRY itself (applying the refusal to the export kills 4 tests), and no
  pre-existing test is masked by the new guard.

## reviewer-security (three EXECUTED bypasses; CLI projection itself judged sound)
G5-28 [HIGH, = G5-01, executed] Case: both `Configs/` and `CONFIGS/` wrote the 9-column
  name-bearing worksheet into the REAL configs directory; the reviewer listed the directory
  afterwards and both files were there.
G5-29 [HIGH, executed] CHECK-ONE-OBJECT, WRITE-ANOTHER. `_refuse_tracked_destination` resolves;
  the write uses the LITERAL path and `os.replace`, which replaces the SYMLINK ITSELF. Measured:
  with `configs/g.csv` a symlink out of the tree, the write was accepted and afterwards
  `configs/g.csv` was a REGULAR FILE of 267 bytes with the full header, while the link target did
  not exist. The `.tmp` staging file also transits `configs/`.
G5-30 [MED, executed] A `configs` that is itself a symlink disables the refusal entirely — the
  exact half-(b) failure journal.py's E-3 boundary already documents and guards.
G5-31 [HIGH, NOT FIXED HERE -> CTO] The refusal protects a directory NAME, not tracked-ness.
  Measured accepted, each writing the name-bearing worksheet: the project root, `docs/`,
  `tests/fixtures/`, `workstreams/lung/`. All tracked. Only `data/` is ignored.
G5-32 [HIGH, NOT FIXED HERE -> CTO] A SECOND, UNGUARDED WRITER with a worse payload:
  `write_compounds` persists canonical_inchikey + drugbank_id + name + inchi + inchikey — the
  COMPLETE record, not just the association — and is reachable from the CLI as
  `chipsim write --out <any path>`, `--out` being unvalidated. Measured: it wrote into `configs/`
  with no refusal. Also `merge_report.main --project-root` writes the id/name/key association into
  a journal path the user chooses (mitigated only by journal/** being git-ignored here).
G5-33 [MED, NOT FIXED HERE -> CTO] The repo-wide guard SKIPS anything that is not UTF-8 text, so a
  tracked `.parquet` carrying a real accession beside a name and an InChI returns NO hits —
  measured against a CSV control that does hit. And there is no `name` detector anywhere, so the
  NAME half of the invariant has exactly one enforcement point in the repo: the column-shape
  refusals in adjudication.py. That is why the bypasses above matter more than their individual
  reachability suggests — there is no second line.
CLEAN per security: the new CLI cannot emit a `name` or an accession (the projection is sound,
  and a duplicated `name` header is mangled by pandas and then refused); nothing in the change
  writes, echoes or journals record content; argv journalling is safe because journal/** is
  git-ignored; the guard suite passes and all four changed paths are in scope and clean; an
  export onto an existing `barrier_panel.yaml` is refused with the YAML byte-identical.

## What I fixed in this gate (84ce8e0) — each verified by mutation, all 11 mutants killed
Case, symlinked directory, symlinked destination, symlink loop (now AdjudicationError, not
RuntimeError), over-breadth (anchored to source_root(), component check applied RELATIVE to the
project root), the CLI's error contract (OSError + pandas read errors -> message + exit 2), the
verdict count in the success line, the vacuous `match="configs"` assertions, the resolution test
that could not distinguish a substring implementation, the parent-only coverage, the unpinned exit
code, the unasserted success output, the untested fail-closed consequence, and the journal
pollution (tests now redirect CHIPSIM_PROJECT_ROOT and assert the record lands in the scratch root
— verified: 38 records before a run, 38 after).
Also fixed: the FALSE RECOVERY INSTRUCTION (G5-11) — the message now says plainly that the tracked
file IS the surviving copy, that there is no automatic recovery, and what to do instead; the stale
"worksheet" wording r2.19 repaired in the plan but not in the code; the public docstring now names
the refusal; and TRACKED_DIR_NAME is private and use-named, with the false "only this module may
write here" claim removed (seal_panel legitimately writes configs/barrier_panel.yaml).
