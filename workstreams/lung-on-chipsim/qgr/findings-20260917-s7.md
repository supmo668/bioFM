# §7 (r2.20 allow-list + r2.21 E6-2/E6-4/E6-5 + r2.22 E6-1b) — findings.
Base 1a5d72f, Hash A f520f12. FULL roster on the throwaway copy (record-content surface).

## own review — two clause requirements I have NOT implemented, found by re-reading the clauses

OWN-1 [HIGH, own] E6-1's PER-PROJECT DECLARATION FILE MECHANISM IS NOT BUILT. The clause says
  declarations live in "per-project declaration data — a file in that project, following this
  module's own DRUGBANK_ID_LEDGER precedent of pointing at `configs/`" and that "THE GUARD READS
  THE UNION", with each entry carrying `path -> sha256` or asserting "derived from tracked source
  S, and S is in scope". What I built removes the foreign paths and empties BINARY_ALLOWLIST,
  leaning entirely on E6-1b's scoping to keep the gate green. That is half the clause: the removal
  half. Today nothing reads a per-project declaration file, so if `perturb-seq-eval` writes one
  tomorrow it has no effect, and the sha256 content-pinning E6-3 folded into this clause does not
  exist anywhere.
  Composition note: E6-1b makes the gate GREEN without the mechanism, which is exactly why this is
  easy to miss — the tests pass, and the missing half is invisible unless you read the clause
  against the code. That is the failure mode this project keeps finding.
  -> Implement the union reader + sha256/derived-from validation, or get it explicitly deferred.
  Do not leave a signed clause half-built with a green suite.

OWN-2 [MED, own] E6-2 requires the two kinds to be "named distinctly so a structured container
  cannot be filed among figures where nobody registers it". My list is now EMPTY and still called
  `BINARY_ALLOWLIST` — a name that says nothing about rendered-vs-container. The distinction the
  clause asks for exists only in a comment. An empty list also reads to a newcomer as "nothing is
  ever declared", so the next person to add an entry has no name telling them what may go in it.
  -> Rename to name the KIND (e.g. `RENDERED_ARTIFACT_DECLARATIONS`) so a container cannot be
  filed there by a reader who never read the comment.

OWN-3 [LOW, own] `THIS_PROJECT = "lung-on-chipsim"` is a module constant in an INGEST module, and
  `_OWNERSHIP_PREFIXES` is now the ownership source of truth for the whole repo, also in that
  module. E6-6 authorises extracting the guard; until then a reader must know that an ingest module
  decides repo-wide ownership. Noted for the extraction, not fixed here.

OWN-4 [LOW, own] I deleted the r2.19 dead code by slicing text and took `class AdjudicationError`
  and a `@pytest.mark.parametrize` decorator with it. Both were caught by the suite and restored
  within minutes — but a text-slice deletion that removes an exception class is a reminder that
  "delete the dead block" is not a safe mechanical edit. Recorded against myself.
## reviewer-design — 26 findings; three verified holes, each the clause's own defect one level down

D7-07 [CRITICAL, VERIFIED] A BINARY NAMED `leak.md` IS STILL DOUBLE-EXEMPT. E6-4's waiver is decided
  by FILENAME, so planting a raw binary blob carrying a real accession at
  `.claude/usr/.../dispatches/leak.md` gives: accession-excluded True, hits [], undecodable [],
  failing [], report []. Nothing listed anywhere. It defeats THREE clauses at once — E6-4 ("the
  waiver covers MESSAGES, not bytes"; a binary is not a message whatever it is named), E6-1b's
  load-bearing annotation, and may-not-skip-silently. The clause text and my code agree with each
  other and are BOTH wrong the same way: they decide messagehood by extension, which this module's
  own §6 parquet lesson already refuted ("dispatch on the MAGIC, not on the name"). Fix: the waiver
  requires the payload to DECODE AS TEXT and match the .md shape. Clause-touching — report it.
D7-08 [HIGH, VERIFIED] `path_owner("projects/x.bin")` returns `'x.bin'` — an INVENTED project, so
  the file is unfailable everywhere. Same for `libs/x.bin`, `workstreams/x.bin`. r2.22 says the
  opposite in as many words: "a path matching no owner is unowned by definition, never somebody
  else's", and the clause's purpose is that scoping can never make a file unfailable. This is the
  hole the CTO caught in his own draft, reappearing one level down in my implementation of it.
  Fix: return None unless the segment names a directory that exists at that level.
D7-09 [HIGH] MY LIVE TEST RE-CREATES THE COUPLING r2.22 REMOVED: `assert owners == {"perturb-seq-
  eval", "paper_standalone"}`. The moment aviary-biosim or cellforge-agents commits an undecodable
  file, THIS gate turns red on another team's artifact — the measured harm the clause quotes. The
  code correctly declines to fail; my test then fails anyway. Fix: assert the anti-vacuity property
  the clause asks for, not an exact owner set.
D7-03 [HIGH] = my OWN-1, sharper: `BINARY_ALLOWLIST` is still "exactly the shape E6-1 forbade — in
  this module's source, keyed by bare path, with no content pinning", and its docstring still
  INSTRUCTS the next maintainer to use it ("adds it here"). Emptying it removed the instances and
  left the mechanism and its instructions. The first person to ship a figure under
  projects/lung-on-chipsim/ re-creates the E6-1 defect with every test green.
D7-04 [HIGH] Nothing tests E6-1's actual invariant: re-adding all 23 foreign paths would pass every
  existing test. E6-1 is enforced by the ABSENCE OF DATA, not by a mechanism. One-line fix:
  `assert path_owner(rel) == THIS_PROJECT` for every declared entry.
D7-12 [HIGH] `chipsim/guards/__init__.py` says "rules about WHERE things may be written and WHAT may
  be read" — the package holds ONLY the write half; the read half is still 200 lines in ingest.
  That is an OVERCLAIM OF COVERAGE BY A GUARD, the precise thing E6-5 forbids, one directory above
  the two docstrings E6-5 made scrupulous — and the E6-5 test inspects the two function docstrings
  only, so the package docstring slips past the test written to catch exactly this.
D7-14 [HIGH] `THIS_PROJECT = "lung-on-chipsim"` is a bare string bound to nothing. Rename the
  project directory and every path this project owns becomes "somebody else's": failing_undeclared
  returns [] and THE GATE GOES GREEN — fail-open, reached by a rename nobody would think of as a
  guard change. Fix: derive from source_root().name, or assert equality with it.
D7-20 [MED] `chipsim write --out configs/x.parquet` exits with a RAW TRACEBACK — OutputRootError is
  caught nowhere — contradicting the rule the same file states for its sibling command. Worse: MY
  TEST asserts in prose that the CLI has an `except OutputRootError` clause WHICH DOES NOT EXIST.
D7-21 [MED] The worksheet writer's refusal type changed from AdjudicationError to OutputRootError,
  so the module's documented error taxonomy no longer covers its own refusals and a caller with
  `except AdjudicationError` leaks. My two writer tests hide it with `pytest.raises(Exception)` —
  the weakest assertion available, which would pass on an unrelated failure raised earlier.
D7-22 [MED] The registry detects writers by METHOD NAME (to_csv/to_parquet only): `to_hdf`,
  `pq.write_table`, `to_feather`, `write_text`, `json.dump`, `csv.writer` are invisible, so a new
  record-bearing writer CAN silently opt out — the property r2.20 names. Irony: E6-2 just taught
  this module to read HDF5, and a `to_hdf` writer would not be seen. Also no test asserts that each
  name in RECORD_BEARING_WRITERS actually calls the helper.
D7-23 [MED] The 256 MiB cap and two generic `except Exception` arms make a magic-identified
  CONTAINER fall back to "undecodable — declare it", which E6-2 forbids and which the MemoryError
  branch's own comment says must never exist. h5ad routinely exceeds the cap.
D7-11 [MED] `libs/<name>/**` is owned by the code but granted by no clause, and the `workstreams/`
  rule sits OUTSIDE `_OWNERSHIP_PREFIXES` — the constant whose docstring calls itself the explicit
  map. A reader auditing the map misses half the rule and sees one extension never granted.
  Shrinking what fails here is an ownership assignment (rule 10).
D7-01 [MED] The samestat-instead-of-case-folding decision is substantively right (verified: no input
  where identity admits something case-folding would refuse) but it REPLACES the mechanism a SIGNED
  clause names, recorded only in a docstring. Escalate a one-line amendment; do not change the code.
D7-05/06 [MED] = my OWN-2, plus: the "no container declared" test filters by SUFFIX — name-based
  dispatch, the anti-pattern this module condemns 170 lines earlier. A container named `blob.dat`
  passes. Test the MAGIC instead.
D7-17/18/19 [MED] The guard depends on declared roots EXISTING (masked by .gitkeep files nothing
  links to it) and refuses a legitimate write with a message listing that root as allowed; roots
  anchor to the installed package tree, so a non-editable install refuses every record-bearing
  write; and the whole system temp dir is allowed in PRODUCTION, while `/tmp` on macOS is refused
  because it is a symlink.
D7-24/25/26 [LOW] Content-exclusions silently buy a listing exemption; a dangling tracked symlink is
  skipped by both halves; an unreadable HDF5 dataset is recorded `<unreadable>` and the container
  still counts as read — "no hits" from something never read, one level down.
CLEAN per the reviewer: E6-5 at the mechanism level (both halves stated, both directions pinned);
  E6-2's substance (footer + per-field metadata, list stringification, deep rows, middle columns,
  index, magic dispatch, HDF5 attrs + string datasets, loud failure without a reader); r2.20's
  writer half (one helper, both writers, literal AND resolved containment, symlinked destination and
  ancestor refused, identity not substring, no second CLI check); E6-1b's listing/failure split;
  E6-4's non-.md cases; approval provenance (plan_hash 6eba1bb == log row 21); dependency direction.
## reviewer-code — every finding EXECUTED; two are my own lessons reappearing in my own fix

C7-01 [HIGH, EXECUTED] THE TMP ROOT IS AN ENV-VAR-REDIRECTABLE SINK. `declared_output_roots()` uses
  `tempfile.gettempdir()`, i.e. `$TMPDIR`. With TMPDIR set to the project root, the ENTIRE TRACKED
  TREE becomes a declared output root and
  `refuse_unless_declared_output_root(<root>/configs/drugbank_compounds.parquet)` ACCEPTED — both
  record-bearing writers may then write accession+name+structure into configs/. CI runners routinely
  set TMPDIR inside the workspace.
  THE PART THAT STINGS: `journal.source_root()`'s own docstring refuses exactly this ("an audit
  trail whose default location can be redirected by an environment variable is one an operator
  cannot reason about"), and the CTO warned me about this precise smell ONE HOUR AGO — four defects
  in two days from "a tool deriving a correctness-relevant identity from ambient shell state". I
  then derived an allow-list root from an environment variable.
C7-02 [HIGH, EXECUTED] THE HDF5 READER RE-INTRODUCES THE NUMPY-REPR ELISION I FIXED FOR PARQUET.
  `parts.append(repr(values))` elides above 1000 elements — the exact defect
  `_parquet_chunks`' docstring says it exists to avoid. An h5ad with 2000 strings and the accession
  at index 1500: hits [] AND undecodable [] — double-clean. On the LIVE tree: the 34.5 MB
  Adamson2016_pilot.h5ad yields 4,270 characters, three elided lines, SIX of ~41,000 identifiers
  actually scanned. The file the reader was added for is ~99.99% UNREAD while reporting clean — and
  its 0.01s scan time, which I quoted as evidence the cost was fine, was a measurement OF the
  elision.
C7-03 [HIGH, EXECUTED] COMPOUND dtype (`kind == "V"`) is skipped — the on-disk shape of every HDF5
  "table" and of `DataFrame.to_hdf(format="table")`. A table whose first row is a real accession
  scans as the literal string 'table': hits [], undecodable []. The parquet-footer failure in a new
  costume, in the reader written to close the parquet-footer failure.
C7-04 [HIGH, EXECUTED] = D7-08, with more cases: `projects/README.pdf` -> owner 'README.pdf',
  `libs/x.bin` -> 'x.bin', `workstreams/notes.bin` -> 'notes.bin', `projects/../configs/x.bin` ->
  '..'. Each fails NOBODY's gate. `git add projects/leak.pdf` with a real accession: listed under a
  fabricated project, failing_undeclared [], live test green, and the accession scan skips it as
  unreadable. The double-exempt hole E6-4 closed, re-opened one function below the comment calling
  unowned-fails-here "LOAD-BEARING FOR E6-4".
C7-05 [MED, EXECUTED] An unreadable DATASET is swallowed as `<unreadable>`, the file then counts as
  READ and scans CLEAN, and nothing inspects the marker. Reproduced with the exact error an h5ad
  written with hdf5plugin raises when the plugin is absent.
C7-06 [MED, EXECUTED] Soft and external links are skipped by `visititems`: a container whose only
  members are links produces an EMPTY chunk and is classified readable and clean. A container
  yielding ZERO bytes should never pass as "read".
C7-07 [MED, EXECUTED] The blanket symlinked-ANCESTOR refusal false-refuses legitimate destinations,
  including the declared roots: `tempfile.mkdtemp()/w.csv` is refused because `/var` is a symlink on
  macOS — the tmp root AS THE MODULE ITSELF COMPUTES IT is unusable when spelled that way; the suite
  only passes because pytest pre-resolves tmp_path. Symlinking data/interim to a data disk (a normal
  arrangement) refuses every worksheet write, naming the declared root as the offender.
C7-08 [MED, EXECUTED] Containment walks LEXICAL parents, so with cwd inside a declared root,
  `"w.csv"` is REFUSED while `"../w.csv"` from the same cwd is ACCEPTED — the verdict depends on
  spelling. The deleted r2.19 helper absolutised first; I dropped that when I replaced it.
C7-09 [MED, EXECUTED] If a declared root does not exist, writes INTO it are refused with "it is not
  inside a declared untracked root" — and both writers mkdir AFTER the guard, so the guard defeats
  the writer's own bootstrap. Latent only because of .gitkeep files nothing links to the guard.
C7-10 [MED, EXECUTED] Any git checkout living under $TMPDIR loses the whole protection:
  `<tmpdir>/checkout/projects/lung-on-chipsim/configs/worksheet.csv` ACCEPTED.
C7-11 [MED, EXECUTED] THE REGISTRY CONTAINS A PHANTOM AND MISSES THE REAL WRITER.
  `merge_report.write_merge_report` DOES NOT EXIST; because the assertion is `found <= declared`, a
  stale declaration passes silently. Meanwhile `merge_report.main` writes via
  `write_text(json.dumps(...))` to an operator-chosen `--out` with NO destination guard — and "a
  tracked merge report came to carry 89 real accessions" is the incident quoted in this very guard's
  docstrings. My registry cannot see the writer shape that caused the incident it cites.
C7-12 [MED, EXECUTED] Unowned = everything outside projects/, libs/, paper_standalone/, workstreams/
  — so docs/, config/, research/, scripts/, tools/, .claude/ ALL fail THIS project's gate. A new
  `docs/architecture.png` from anyone breaks my suite, and the only remedy (BINARY_ALLOWLIST) is the
  one my own comment forbids. Also: my comment claims the 23 "fail their owner's gate rather than
  this one" — NO SUCH GATE EXISTS anywhere (grep confirms), so they now fail NOWHERE. I wrote a
  reassurance about a mechanism that does not exist.
C7-13 [MED] The worksheet refusal type changed to OutputRootError, so `except AdjudicationError`
  no longer catches it; the docstring still describes the deleted r2.19 rule; and
  `chipsim write --out configs/x.parquet` arrives as a TRACEBACK journalled as "crashed".
C7-14 [LOW] `target.open("rb").read(8)` leaves the handle unclosed — the only unmanaged open() in
  the scan path, and a line this diff touched.
C7-15 [INFO] `except (MemoryError, RuntimeError): raise` is broader than its stated purpose; h5py
  surfaces some HDF5 errors as RuntimeError, which would abort the whole scan. Reasoned, not
  measured (could not produce one with h5py 3.16).
CLEAN per the reviewer, each checked rather than assumed: `_is_within` core semantics all fail SAFE
  (absent destination, no existing ancestor, ancestor-is-a-file, mid-walk race); the literal AND
  resolved conjunction is LOAD-BEARING — removing either half re-opens r2.19's bypass; every other
  path_owner input fails safe (bare `projects/`, Windows separators, leading `..`, absolute,
  capitalised, empty, double slashes) and `.claude/**/dispatches/leak.pdf` -> None -> fails here, so
  the E6-4 claim holds for THAT path; root-group attributes ARE covered (visititems never visits
  '/'); an exception inside visit does NOT abort silently — it propagates and the file is REPORTED;
  short files (0, 2, 4 bytes) behave correctly under read(8)+startswith; the r2.19 deletion removed
  EXACTLY three symbols and nothing else, with no dangling references, and
  write_adjudication_worksheet's body is byte-identical apart from the one-line call swap; the .tmp
  sibling sits inside the validated directory so the atomic write does not evade the guard;
  undeclared_report/failing_undeclared agree by construction, are sorted, and cannot raise on
  duplicates; performance is fine today (report 0.04s, hits 0.23s over 767 files) — but the h5ad's
  0.01s is a measurement of the elision in C7-02 and will rise when it is fixed.
## reviewer-security — 7 HIGH, all EXECUTED. Four independent paths to a tracked file.

S7-01 [HIGH] = C7-02 HDF5 repr elision, with the live artifact measured: 34.6 MB h5ad, 4,270 chars
  scanned, 3 elision markers; var/gene_symbol (35,635), var/ensembl_id (35,635) and obs/cell_barcode
  (5,768) ALL elided; only */categories (1-9 elements) fully read. <0.5% of its string content is
  examined. "The container is always READ" is false for every real container, including the repo's.
S7-02 [HIGH] Compound (`kind == "V"`), numeric and external-link content is skipped while the file
  still counts as READ. A compound dataset holding the whole record scanned to 7 CHARS. And they
  planted the end-to-end case in THIS PROJECT'S OWN PATHS: projects/lung-on-chipsim/docs/codes.h5
  and docs/pilot.h5ad both give hits [], report [], failing [] while the record IS in the bytes —
  so the scoping never even applies: MY OWN GATE PASSES ON MY OWN RECORD-BEARING FILE.
S7-03 [HIGH, EXECUTED] THE `.tmp` SIBLING IS NEVER CHECKED — bypass #3 moved one filename over.
  The guard validates `out`; the writer then writes `out.name + ".tmp"`, and `to_csv` FOLLOWS a
  symlink. Pre-place data/interim/pgp_adjudication.csv.tmp as a link to configs/stolen.csv, then
  make a PERFECTLY LEGITIMATE, fully-allowed call — rows=1, no refusal, and the tracked path now
  holds `FIXTURECMPDAAA-FIXTUREKEY-N,SECRET-DRUG-NAME,...`. Literally the check-one-object-
  write-another bypass the r2.20 docstring claims to have closed.
S7-04 [HIGH, EXECUTED] HARDLINKS ARE NOT REFUSED (only symlinks are). `os.link(configs/victim.csv,
  data/interim/hard.parquet)` then `write_compounds(...)` → ALLOWED, and the tracked file now begins
  b'PAR1', 5,197 bytes — the worst payload in the project written into configs/ with no symlink
  anywhere. `write_compounds` writes IN PLACE, so no rename breaks the link.
S7-05 [HIGH, EXECUTED] = C7-01 TMPDIR, with a second case: TMPDIR=/private/tmp with the checkout
  living under it makes every tracked path of that checkout writable. CI runners that clone into
  $TMPDIR or mktemp -d hit this with no attacker at all.
S7-06 [HIGH, EXECUTED] THE "UNTRACKED" ROOTS CONTAIN GIT-TRACKED PATTERNS. .gitignore re-includes
  `!data/**/*.dvc` and `!data/processed/*.sha256`; verified `git check-ignore` exits 1 for
  data/processed/pgp_adjudication.sha256. Writing the name-bearing worksheet THERE is ALLOWED —
  no symlink, no env var, no race, just a filename inside a declared root, in the directory where
  .sha256 is this project's own sidecar convention. The allow-list approximates "untracked" BY
  DIRECTORY; the ignore file makes that false BY SUFFIX.
S7-07 [HIGH, EXECUTED] = D7-07 `.md`-named binary double-exempt: leak.pdf → waived False, listed
  True, fails True (correct); leak.md → waived True, listed False, fails False. Contradicts the
  module's own doctrine two functions away ("dispatch on the MAGIC, not on the name").
S7-08 [MED] "Fails their owner's gate" is FICTIONAL — grep confirms no other project implements it,
  so those files fail NOWHERE; and path_owner accepts any segment: `projects/lung-on-chipsim-evil`,
  `workstreams/not-a-project`, and `projects/../projects/lung-on-chipsim/leak.bin` → '..', which
  mis-attributes one of THIS project's own files away from this gate. A planted
  projects/perturb-seq-eval/artifacts/leak.bin hides among the 23 existing entries with the live
  test green.
S7-09 [MED] A declared root that is ITSELF a symlink silently extends the allow-list to its target:
  data/interim → configs/ makes configs/ a declared root. Benign trigger: relocating data/interim
  to another disk grants that whole disk.
S7-10 [MED] The HDF5 reader has NO memory bound (parquet has _PARQUET_BATCH_ROWS; _MAX_SCAN_BYTES
  bounds only the ON-DISK size). A 206 KB gzip-compressed dataset materialised 200 MB — 950x — while
  scanning 6,135 chars. A 250 MB tracked container scales to ~230 GB. MemoryError is re-raised, but
  an OOM-kill is SIGKILL, and this module's own parquet comment says a guard that OOMs gives no
  verdict at all.
S7-11 [MED] `_cell` elides a top-level STRUCT column's VALUES (iterating a dict yields keys):
  scanned as `[accession,name,inchi]` while the record is in the bytes. list<struct>, map,
  dictionary-encoded and multi-row-group are all CAUGHT — this is the one remaining nested shape.
S7-12/13/14 [LOW/INFO] Link-only container yields zero chars yet is "readable"; on macOS the
  natural $TMPDIR spelling is always refused while /private/var is allowed; and every tracked file
  with HDF5 magic is now fed to libhdf5 (a C parser with a long CVE history) on every test run,
  including files arriving via PRs — corrupt/truncated inputs verified to fail safe.
CLEAN per security, extensively probed: every PATH-SHAPE attack on the allow-list failed —
  `..` traversal, cwd-relative bare names, symlinked destination and ancestor, symlink loop
  (OutputRootError not RuntimeError), substring roots, deleted declared root (fail-closed). The
  literal-AND-resolved double check defeated every path trick constructed; its weaknesses are the
  FOUR NON-PATH vectors above. Parquet: footer/per-field metadata, dictionary encoding, 50
  row-groups with the hit only in the last, deep rows, middle columns, the index, 2,000-element list
  cells, list<struct>, map, and magic dispatch — all caught. HDF5: root and per-node attributes,
  group/dataset names, string datasets <=1,000 elements; corrupt files reported, missing reader
  raises loudly. E6-4/E6-1b as specified work, including a nested non-.md payload. No new network,
  no shell=True, no secrets, no eval, no untrusted pickle.
