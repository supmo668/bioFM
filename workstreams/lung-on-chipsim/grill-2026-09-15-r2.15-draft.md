## 7c · Amendment r2.14 → r2.15 — principal grill 2026-09-15 (nine decisions, structured 1B1, CTO session biofm-14)

Decisions are the principal's; wording is the CTO's. Each item names the plan text it changes.
No task added or removed. Items marked *(editorial)* correct the plan to match records that
already exist; items marked *(ruling)* change what the plan requires.

**1 · Human-artifact authorship — split by kind** *(ruling; changes T1, T18, T14)*
- `PROVENANCE.md` (T1's prose half): the **CTO drafts, the principal ratifies and seals** under the
  T8 pattern (`ratified:` block naming the human, date, and the digest over the ratified text).
  Rationale, so the exception is not read as precedent: the *decision* it explains was already made
  by the principal (1B1 2026-09-09) and is recorded verbatim in `provenance.yaml`
  (`non_commercial_commitment`); the prose restates a decided claim rather than making one. The
  Architecture rule — "a human owns anything whose output is a *claim*" — is preserved by the
  ratification step, which is the act that turns the draft into the human's own words. T1's
  "hand-written" is amended to "human-ratified; may be agent-drafted".
- `configs/poc_compounds.yaml` (T18) and `configs/pgp_adjudication.csv` (T14): **principal-authored,
  no agent draft.** The agent supplies only the schema (`roster-schema.md`), the validator (S11a),
  and the candidate list. Unchanged from r2 — restated because item 2 makes the hand-off explicit.

**2 · Roster inputs wait for the stereo guard** *(ruling; adds a hand-off to T18)*
T18's candidate list is generated **only after** the r2.12 stereo guard has landed through a gate
and the disagreement report is regenerated on guarded keys. The principal authors the roster on
keys that will not move again in slice 1. Entries are keyed by `canonical_inchikey` as before;
`drugbank_id` is carried alongside for traceability, not as the key.

**3 · One interactive session per tree** *(ruling; new Global Constraint)*
A worktree has exactly one interactive session that may commit to it; the trunk has exactly one
CTO session that may write to it. A second session on the same tree is the signing hold: the tip
moves mid-computation and no receipt verifies. Resolution on 2026-09-15: the bare session on the
lung-on-chipsim worktree (pid 56186, since 2026-09-09) is closed; the named agent session is the
sole writer. On the trunk, session biofm-14 is the CTO of record; the bare session on ttys002
(pid 51059) is closed after it commits what it holds. Its rulings survive on disk (dispatches
1331/1526/1542 of 2026-09-14; 0141/1221/1232/1241 of 2026-09-15; plan r2.10–r2.13).

**4 · Agent sequence after the hold lifts** *(ruling; §4 Execution order)*
(i) the gate pass on the 57 findings, including replacing the halt-rule tautologies over
`evaluate_halt`'s return value with tests that can fail — **first**, because building on a bed
with checks-that-cannot-fail repeats the fixture-vs-reality class; (ii) the r2.12 stereo guard;
(iii) T4 as amended in r2.11 (`dvc add` ×3, test updates); (iv) the two small records — the C4
third-site note in `T8-review-record.md`, and `airway_evidence:` into the panel schema. Each lands
through its own gated commit. T11 waits on item 1's PROVENANCE ratification.

**5 · Panel signing deferred to M1** *(ruling; Global Constraint 4, T8)*
Cryptographic signing of `barrier_panel.yaml` (minisign against a pinned human key) happens at the
**M1 re-ratification**, when the three provisional faces (TFRC/FCGRT/SLCO2B1) are re-checked, so
the panel is signed once, settled. Until then the plan, the run journal's provenance block (T17)
and any model card carry verbatim: **"digest-sealed, human-ratified, not cryptographically
signed."** Constraint 4's "with the principal, undecided" becomes "decided: M1, minisign".

**6 · Approval provenance lives in an append-only log** *(ruling; new Global Constraint)*
`plan-approval.md` is tool-owned and carries only what `plan-gate sign` writes. A sibling
`plan-approval-log.md` is **append-only**: one entry per sign — revision, hash, route
(human-direct / standing-delegation), authorising rulings, prior human-direct hash. Every sign
appends an entry; the quality gate fails if the newest entry's hash differs from
`plan-approval.md`'s. The log is seeded from git history of the approval file back to r2 (737a8d9).

**7 · Principal's authoring window** *(ruling; new milestone under T18/T14)*
Counted from the day the guarded candidate list is handed over: roster by working day 2,
P-gp adjudication by working day 3. The plan carries the hand-off date and the two due dates as a
named milestone; a slip is reported in the dev-log, never back-filled by an agent draft.

**8 · AM-6** *(editorial; §7 pointer at "AM-6 OPEN")*
The decision is resolved by ADR-0002 (accepted 2026-09-02). What remains open is the count-check
against real M0b records, which cannot happen before curation. Pointer reworded: "AM-6 resolved
(ADR-0002); arithmetic re-checked at M0b against real counts."

**9 · `theta_priors.yaml` is not a missing artifact** *(editorial; standing block in CTO directives)*
S6's done-condition requires it **not** to exist in slice 1. The standing "four human artifacts
absent" line is corrected to three: `PROVENANCE.md`, the roster, the P-gp adjudication.

Closed without a ruling, recorded for completeness: the TTY gate is already a confirmation
**read** (E-4, `pipeline.py:231-243`), not a bare `isatty()`; the map's "passive" item was stale.
