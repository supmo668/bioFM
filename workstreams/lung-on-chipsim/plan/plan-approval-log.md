# Plan approval log — lung-on-chipsim

**Append-only.** One entry per `plan-gate sign`, newest last. `plan-approval.md` beside this file
is tool-owned and is rewritten from scratch on every sign; **this file is the provenance**. The
quality gate fails when the newest entry's `hash` differs from `plan-approval.md`'s `plan_hash`.
Routes: `human-direct` (the principal signed or instructed the sign in so many words) ·
`standing-delegation` (CTO invoked under the delegation recorded 2026-09-10, `fd2c00d`; the
judgement was the principal's, the invocation was not) · `principal-directed` (pre-delegation
form of the same thing). Introduced by r2.15 item 6; entries before it are reconstructed from
`git log -p plan-approval.md` and marked *(reconstructed)*.

| # | rev | hash | route | signed | commit | authorising rulings / notes |
|---|-----|------|-------|--------|--------|-----------------------------|
| 1 | r2 | `737a8d9` | human-direct | 2026-08-30 16:53 | `1132fda` | G4 "Over and out" in /grill-me; build authorized. Last **human-direct** hash until #10. *(reconstructed)* |
| 2 | r2.x | `b79a5e4` | principal-directed | 2026-08-31 | `817485c` | re-sign notified to agent; standing rule on hash-locked plan. *(reconstructed)* |
| 3 | r2.5/6 | `a44e523` → `594301b` | principal-directed | 2026-09-01 20:05 / 21:04 | `76f494f` / `f3dc780` | reframe hold; S12 landing. *(reconstructed)* |
| 4 | r2.7 | `bc61506` | principal-directed, cto-invoked | 2026-09-03 01:56 | `a9c8629` | signed on principal instruction; B4 provenance block added. 02:08 agent re-sign stripped B4 (incident, `d4151eb`); restored `8d3df2c`/`7c21302`. *(reconstructed)* |
| 5 | r2.8 | `373931c` | standing-delegation | 2026-09-10 12:40 | `fd2c00d` | delegation of `plan-gate sign` recorded — invocation delegated, judgement is not; panel seal explicitly excluded. *(reconstructed)* |
| 6 | r2.9 | `db3d10b` | standing-delegation | 2026-09-14 13:28 | `d5b243f` | 10th reframe survivor + C4 uv-run fix; 8 unparseable compounds ruled. *(reconstructed)* |
| 7 | r2.10 | `de4b812` | standing-delegation | 2026-09-14 15:24 | `d191421` | principal 2026-09-14 organism-label ruling (accept `Human` and `Homo sapiens`). Marker stripped a 3rd time and restored. *(reconstructed)* |
| 8 | r2.10 | `de4b812` | **human-direct** | 2026-09-15 01:01 | `0b8d0c3` | principal: "approve r2.10 sign". Same hash as #7; supersedes its route. Prior human-direct: `737a8d9`. |
| 9 | r2.11 | `26b7a4f` | standing-delegation | 2026-09-15 01:40 | `3d8be06` | principal 2026-09-15 T4 ruling: three per-file DVC pointers. Marker stripped a 5th time and restored. *(reconstructed)* |
| 10 | r2.12 | `16b0cc9` | standing-delegation | 2026-09-15 11:19 | `d230c3b` | principal 2026-09-15 stereo-layer ruling: `/t /m /s` only, `/b` excluded. Marker stripped a 6th time and restored. *(reconstructed)* |
| 11 | r2.13 | `ebc5542` | standing-delegation | 2026-09-15 12:43 | `a673207` | T5b names L-threonine vs D-allothreonine (the snapshot's *D-Threonine* row is D-allothreonine, `AYFVYJQAPQTCCC-PWNYCUMCSA-N`). Marker stripped a 7th time; no-ruling gap disclosed. *(reconstructed)* **Corrected 2026-09-16 under r2.16 (dispatch #131): one DrugBank accession removed from this row; nothing else changed.** Edit made by the concurrent CTO session `9f83b99d`; the landed r2.16 is row 15, signed by the lane-owning session `b3a61f59`. |
| 12 | r2.14 | `d83fceb` | standing-delegation | 2026-09-15 15:16 | `6add2ec` | principal 2026-09-15 relative-stereo ruling (`/s2` keyed stereo-free + `stereo_is_relative` flag) and the 2026-09-15 stereo-layer ruling (`/t /m /s`, `/b` excluded); QG-11. Also records the measured source-label defect (11 genuine `D-`/`L-` errors on absolute rows, 1 old-keying artifact). **Not reconstructed — corrected 2026-09-15 by the signing session** (`b3a61f59`): the seeded row carried the commit time 15:19 as the sign time and omitted the authorising rulings. Supersedes r2.13's threonine naming. |
| 13 | r2.15 | `67a1897` | standing-delegation | 2026-09-15 16:15 | _(the commit carrying this row)_ | folded by the lane-owning CTO session (`b3a61f59`, pid 51059) after the principal ruled it keeps the lane. **Item 1 DECLINED by the principal** — `PROVENANCE.md` stays human-authored; the CTO-drafted draft was removed at `6557487`. **Item 3 folded as principle, its 2026-09-15 application recorded VOID** (the "close pid 56186 / 51059" resolution rested on a false premise, disproved by `ps`/`ListAgents`). Original grill scope: artifact authorship split; roster after guard; one session per tree; sequence; panel signing at M1; this log; principal's 3-day window; AM-6 and theta_priors editorial. |
| 14 | r2.16 | `24e3a52` **NEVER LANDED** | standing-delegation | 2026-09-16 14:04 | _(no commit — superseded by row 15 before any commit)_ | **wording only** — DrugBank accessions removed from the T5b r2.13 note (3) and from row 11 of this log (1); compounds named by record title and pinned by PubChem InChIKey (D-allothreonine CID 90624 `AYFVYJQAPQTCCC-PWNYCUMCSA-N`, D-threonine CID 69435 `AYFVYJQAPQTCCC-STHAYSLISA-N`). CTO ruling 2026-09-16 on dispatch #131, under the principal's 2026-09-15 record-content re-ruling (#120) and the #122 §3 no-accessions-in-coordination-records rule. Plan forward-fixed (a spec people copy from); log row corrected in place with disclosure (row-12 precedent); **no exclusion for plan files in the guard test**. No condition, task or figure changed; **no principal ruling of its own** (r2.8/r2.9 truth-fix precedent). Marker wiped a 10th time and restored from capture. Prior human-direct: `de4b812`. |
| 15 | r2.16 | `33b43a9` | standing-delegation | 2026-09-16 14:08 | _(the commit carrying this row)_ | **The landed r2.16.** Re-signed by the lane-owning session `b3a61f59` after row 14's `24e3a52` never reached a commit. Carries BOTH changes: the concurrent session's accession removal (r2.13 note + row 11) AND the figures correction — r2.12's 1,599/191→156/41 marked **guard-only, pre-re-key**, with `reports/2026-09-15-stereo-guard-tms/merge_report.json` named the source of record (1,576/192→154/43 with the re-key). CTO ruling 2026-09-16 on #131; principal's 2026-09-15 record-content re-ruling; #122 §3. **No principal ruling of its own** (r2.8/r2.9 truth-fix precedent). Marker wiped an 11th time and restored **onto the post-sign hash** — row 14's restore used a pre-sign capture and regressed `plan_hash` to `67a1897`, which is now a standing-delegation rule (marker §6). Prior human-direct: `de4b812`. |
| 16 | r2.17 | `0745f2f` | standing-delegation | 2026-09-16 15:00 | _(the commit carrying this row)_ | **Two halves, deliberately distinguished.** (a) **A real done-condition change:** T14's tracked `configs/pgp_adjudication.csv` drops the `name` column — CTO ruling on QG finding **F-05**, implementing the principal's 2026-09-15 record-content re-ruling; the generated, untracked worksheet keeps `name` so the reviewer can still work. Defect 23's "tracked and attributable" survives via key + verdict + DOI + attribution. (b) **Truth-fix, no ruling:** T13/T15 interfaces had drifted from shipped code (generated columns, tri-state `label_disagrees_with_key`, T15's legacy raise) — r2.8/r2.9 precedent; the agent flagged rather than edited a hash-locked file. Prompted by the §2 boundary `0c4e181`, receipt `061f12d` **CTO-verified in the worktree** (1 of 14), suite 663/4, guard green, push cleared in writing. Also records the agent's own correction of #130 — F-01 was a false-`agrees` path after all — and the CTO's share of it: verifying a function's guards is not verifying its caller's aggregation (now delegation rule 8). Marker wiped a 12th time, restored **onto the post-sign hash**. Prior human-direct: `de4b812`. |
| 17 | r2.18 | `2325057` | standing-delegation | 2026-09-16 15:04 | _(the commit carrying this row)_ | **Repairs a contradiction r2.17 introduced.** r2.17 amended T14 (tracked file = five columns, no `name`) and T15 (raise if the *worksheet* lacks `stereo_is_relative`) in the SAME revision without reading them against each other; the five-column file has no such column, so T15 would have raised on every well-formed input. No runtime breakage — nothing wired T15 in yet — but two signed clauses contradicted. **Ruled on escalation #135, option A as the agent recommended:** T15 reads the TRACKED file and RECOMPUTES the flag from `compounds` (generated, never carried, never stale), raising if `compounds` lacks the flag or an adjudicated key is unknown; T14 gains `export_tracked_adjudication`, projecting the worksheet to the five columns and REFUSING extra human-added columns. B rejected (unversions the source of truth — defect 23 again); C rejected (tracks a generated column). **This is the plan-level twin of QG finding F-01** — correct in the part, wrong in the composition — so delegation rule 8 is widened: two clauses amended in one revision must be read against each other before signing. The agent held all code until the ruling, correctly. **No principal ruling of its own** (r2.8/r2.9 truth-fix precedent). Marker wiped a 13th time, restored onto the post-sign hash. Prior human-direct: `de4b812`. |
| 18 | r2.19 | `7b29fb5` | standing-delegation | 2026-09-16 15:38 | _(the commit carrying this row)_ | **The §3 quality gate's plan-level findings.** G-01 is the one to remember: before the refusal existed, a BLANK worksheet exported over the filled tracked file left **0 of 24 verdicts and returned 24** — destruction reporting success, on 60–90 minutes of irreplaceable human judgement, needing no carelessness (lose `data/interim/`, T13 regenerates blank without error, re-export destroys it). Defect 22 guarded the worksheet and left the published record unguarded. Rulings: **G-15** refuse any resolved path under `configs/` **by path, not by shelling to `git ls-files`** from library code; the export becomes the only writer of the tracked path, which makes r2.18's split load-bearing. **G-17** the extra-column refusal becomes a stated clause. **G-18** a CLI entry on the `chipsim panel-seal` precedent (C4). **G-16/G-19** T15's Done-when names the tracked file and requires `stereo_is_relative` to survive the round-trip. **G-20** no compound name in a cell — stated as an instruction and **recorded as unenforceable by column checks**. **Re-signed once:** the first signature `732642b` never landed — the rule-8 composition check then found a FOURTH stale "worksheet" clause inside T15's docstring, the same drift r2.19 exists to repair. Rule 8 widened again: sweep the whole task for any term a revision redefines. Mutation testing adopted, **on a copy** — two reviewers mutated the tree the gate was hashing and one produced a HIGH finding against a state that never existed in any commit. **No principal ruling of its own.** Marker wiped a 15th time, restored onto the post-sign hash. Prior human-direct: `de4b812`. |
| 19 | r2.20 | `07fd0f6` | standing-delegation | 2026-09-16 18:28 | _(the commit carrying this row)_ | **Corrects the SHAPE of the CTO's own r2.19 clause, on bypasses the §5 reviewers EXECUTED.** r2.19 refused writes to a directory named `configs/`; the roster then executed three bypasses — `CONFIGS/` (which on a case-insensitive volume landed the name-bearing worksheet in the REAL `configs/`), a symlinked `configs` directory that `resolve()` erased, and check-one-object-write-another where `os.replace` swapped a destination symlink for a real file inside the tracked directory — while the same rule was simultaneously TOO BROAD, refusing every worksheet write under any `configs` ancestor including the recovery path. **E-1/E-2:** record-bearing writers (`write_adjudication_worksheet` AND `write_compounds` — the latter persisting accession + name + InChI + InChIKey ON ONE ROW, the complete record, reachable via `chipsim write --out` with no validation) now validate destinations against an **allow-list of untracked roots** through ONE shared helper: literal and resolved paths, case-insensitive, symlinked destinations and ancestors refused, containment not substring, plus a **registry test** so a new writer cannot opt out. **E-3:** the repo guard may not skip an undecodable file silently — a tracked parquet carrying the full association returned NO hits, measured, against a CSV control that did; parquet is now read and scanned. **E-4:** the export's fail-closed journalling is STATED rather than inherited from tuple membership. **E-5:** T14 publishes via `chipsim adjudication-export`, not the hand-move r2.19 itself called the accident. **E-6:** test-written journal entries are KEPT with a note — deleting audit entries to look pristine is the worse defect. Delegation **rule 9** added: a rule that protects a NAME cannot protect an INVARIANT. Process now structural: reviewers on a throwaway copy; clean `git status`/`git diff` asserted before Hash A and after the last reviewer; never hash, commit or push while a reviewer is live. **No principal ruling of its own.** Marker wiped a 16th time, restored onto the post-sign hash. Prior human-direct: `de4b812`. |
| 20 | r2.21 | `8be2e69` | standing-delegation | 2026-09-16 18:59 | _(the commit carrying this row)_ | **§6's gate found a false-clean rebuilt one layer beneath the fix for false-cleans.** A parquet's FOOTER METADATA carried accession + coined name + structure while the file scanned as `",harmless\n0,1\n"` — reported by neither half, certified clean by the guard's own green tests; metadata stamping is ordinary and several engines do it by default. Also measured: numpy eliding list cells past 1000 (an accession at index 1500 of a `groups` list vanished, and `write_compounds` persists list columns); latin-1/UTF-16 text classified "undecodable", which would have made a PLAIN-TEXT carrier need a declaration; parquet dispatched on FILENAME so `UP.PARQUET` and an extensionless blob fell through — the same case-sensitivity shape closed three commits earlier; and 30 KiB expanding to 186.9 MiB with `MemoryError` reclassified as "declare it". **Rulings:** **E6-1** declarations move to per-project data keyed `path -> sha256` or asserting "derived from tracked source S, S in scope" — 24 paths owned by `perturb-seq-eval`/`paper_standalone` had been declared inside THIS module, so another team's figure turned this gate red and the repair landed in a file they neither own nor can judge. **E6-2** readable structured containers (parquet, HDF5, h5ad) are always READ — parquet including footer metadata; only rendered artifacts may be declared, named distinctly. **E6-3** content pinning folded into E6-1. **E6-4** the `#122 §3` dispatch waiver covers `.md` MESSAGES only — a tracked `dispatches/leak.pdf` with a real accession was double-exempt, suite green. **E6-5** the scan enforces the ACCESSION half and the r2.20 writer helper the NAME half, neither claiming the other — narrowing a CTO-accepted §5 claim. **E6-6/E6-7** module extraction and a combined entry point with the FAIL in the API, authorised. **Process vindicated:** reviewers on a throwaway copy — four reviewers, 21 mutants in one review, all four independently confirming byte-identity, after three rounds of written protocol had failed. Delegation **rule 10** added: a decision that looks like bookkeeping is still a shape decision if it assigns ownership — the agent escalated this class one dispatch earlier, then decided it unilaterally, and said so unprompted. **No principal ruling of its own.** Marker wiped a 17th time, restored onto the post-sign hash. Prior human-direct: `de4b812`. |
| 21 | r2.22 | `6eba1bb` | standing-delegation | 2026-09-17 02:40 | _(the commit carrying this row)_ | **The agent ran the composition check I had just ordered, and aimed it at my own revision.** One dispatch after I wrote "read the clauses you touch against each other, because r2.17 shipped two individually-correct clauses that contradicted one another and I signed it", it found that r2.21's **E6-1** did not compose with **r2.20**'s fail-closed clause: of 24 declared binary paths **0 belong to this project** (CTO-verified: 11 `paper_standalone/`, 13 `projects/perturb-seq-eval/` — the agent said 12/12, corrected here because I checked), so removing them as E6-1 required would make THIS module's live test fail on 24 files owned by two other teams. **E6-1 inverted the coupling instead of removing it** — before, another team ADDING a figure turned this gate red; after, another team NOT YET ADOPTING the rule turned it red on day one. **Ruling (E6-1b):** the undeclared-undecodable **FAILURE** is scoped to the owning project; the **LISTING** stays repo-wide with the owner named in a report the test asserts on; the **accession scan does not shrink**; and **a path owned by NO project fails THIS gate**. That last part was **my own near-miss**: E6-1b as first drafted would have made `.claude/usr/**/dispatches/leak.pdf` listed but unfailable anywhere, silently re-opening the hole **E6-4 closed one clause above** — found by reading the two against each other before signing, and the clause now carries an annotation saying why it is load-bearing. **Residual risk written into the plan, not argued away:** a genuinely undecodable file outside this module's paths is neither read nor declaration-gated here; E6-2 shrinks that set to rendered artifacts only. **The agent declined to choose the scope itself under rule 10** — the scope of a failure is an ownership assignment, therefore a shape decision, therefore mine; the rule applied by the party it constrains, one revision after it was written. **Delegation rule 11 added:** a remediation is reported only after it is OBSERVED, never from intent — the agent wrote "I re-armed the monitor" before doing so (self-corrected unprompted, Stop hook caught it), and I wrote that this session "did not authorise a push and cannot see the event" when the clearance and four compliance reports sat in my own resolved dispatch list. **No principal ruling.** Marker wiped an 18th time, restored onto the post-sign hash. Prior human-direct: `de4b812`. |
| 22 | r2.23 | `78d8003` | standing-delegation | 2026-09-17 04:55 | _(the commit carrying this row)_ | **§7's gate EXECUTED four ways a record-bearing payload still reached a tracked path despite r2.20**, three needing no attacker: the **`.tmp` sibling** — the guard validated `out`, the writer wrote `out.name + ".tmp"`, a predictable name, and `to_csv` FOLLOWS a symlink, so a fully-allowed call put the name-bearing worksheet in `configs/`; that is **bypass #3 from the r2.19 post-mortem moved one filename over, inside the guard written to close it**. Also **hard links** (refused nowhere, tracked file overwritten in place with the complete record); the **tmp root read from `$TMPDIR`** (point it at the project and the whole tracked tree becomes a declared output root — the ambient-state family the CTO had flagged an hour earlier); and **"untracked" roots containing tracked names** (`.gitignore` re-includes `*.dvc`, `.gitkeep`, `*.sha256` — untracked by DIRECTORY, tracked by SUFFIX). **"Containers are always read" was false for every real container:** the repo's own 34.6 MB `h5ad` scanned 4,270 of ~1.2M characters — six of ~41,000 identifiers — because `repr` elides above 1,000 elements, **the identical defect `_parquet_chunks` fixed ONE CLAUSE EARLIER**; the agent also retracted its own evidence, since the 0.01s it had quoted as proof the cost was fine **was a measurement of the elision**. **Three defects in its own tests:** the writer registry saw two method names, so a CLI persisting accession + name + InChI + InChIKey via `pq.write_table` **survived the full suite** (widening it surfaced SIX invisible writers); a **phantom declaration** for a function that does not exist, passing because the assertion was `found <= declared`; and a symlink test that **passed on its own `tmp_path` directory name**. **Rulings:** **E-01** operator-chosen `--dest`/`--out` go through the same helper, with `data/raw/` and the journal declared — they were always untracked and merely undeclared, which left the two most record-bearing writers invisible. **E-02** the declaration DATA is acknowledged **still unbuilt**; build it for this project and the repo-root surface only. **E-03** *"fails their owner's gate" is FICTION* — no other project implements this gate, so the 23 fail NOWHERE; this corrects a CTO sentence from r2.22. **E-04/E-06 ruled together** — directory IDENTITY replaces "case-insensitively" (for an ALLOW-list, folding is the PERMISSIVE direction), and identity is what creates the missing-root failure, so two failures now carry two messages. **E-05** unowned paths get a repo-root DECLARATION SURFACE, or the rule has no remedy (rule 9: state where it IS permitted). **E-07** roots anchor at RUNTIME, not to the installed package tree. **E-08, a CTO finding, BLOCKING:** the shipped `chipsim record-content-report` passed `project_root()` and printed *"0 — every tracked file was read"* while **23 files had never been read** — the reporting surface built to prevent a false clean produced one; `#122` had ruled this exact defect for the accession scan. CTO-verified by invoking both roots: project → 0, repo → 23. **Delegation rule 12 added:** implementing a clause is where that clause's own lesson gets rebuilt — check new code against the lesson, not only the clause; test-level instance, *never assert on a string the fixture also produces*. **No principal ruling.** Marker wiped a 19th time, restored onto the post-sign hash. Prior human-direct: `de4b812`. |
| 23 | r2.24 | `db3d598` | standing-delegation | 2026-09-17 05:45 | _(the commit carrying this row)_ | **THE E-08 FIX CONTAINED E-08 — rule 12 demonstrated on itself one revision after it was written.** The agent corrected the root SELECTION and left the root VALIDATION and the file LISTING able to fail silently; they compose: no `.git` above the package → silent fallback to the narrow root **E-08 is** → `git ls-files` fails there → the listing swallows it and returns `[]` → **"0 (failing this gate: 0)", exit 0, printed with the TRUE repo root interpolated**. Reproduced end-to-end through the shipped CLI by the agent and independently by all four reviewers; no attacker needed (non-editable install, root-owned CI checkout refused by `safe.directory`, corrupt index, shimmed `git`). **The anti-vacuity guard for exactly this already existed IN THE TESTS AND NOT IN THE COMMAND** — `check=True`, a count floor, every path resolving — beneath a test titled *"a scan over the wrong or an empty list reports clean"*. **A test named after the regression could not detect it:** a reviewer reverted only the call site and `test_the_report_scans_the_repo_root_not_the_project_root` stayed GREEN, because it passed `root` explicitly and asserted on a string the same commit had deleted; deleted rather than repaired, replaced by a command-equals-function equality holding at 23 and at 0 instead of a `>=20` floor under a number this mechanism exists to drive to ZERO. **Three bypasses survived a correct root, each executed:** `GIT_DIR`/`GIT_INDEX_FILE` steering the listing to a FOREIGN index while the printed root stayed correct (more misleading than the bug being fixed); `core.fsmonitor`, config the scanned tree supplies and git EXECUTES, running as the invoking user — both halves of the `#44` B2 ruling now satisfied; and an owner **minted with `mkdir`**, which under E-03 is strictly better for an attacker than a real owner. Also a filename beginning `ESC[2J` drew a complete fake all-clear over the real report. **Rulings:** **E-09** topology placement folds into E6-6 (placement reassigns ownership of a primitive — rule 10 — and doing it now would force the authorised extraction to undo it); the agent recommended exactly this and did not act unilaterally. **E-10** an unresolvable tracked path is **counted and reported ALWAYS** and **fails only when we own it** — scoping the failure is E6-1b, scoping the count would repeat E-08; fatal-always would make the report unrunnable in a legitimate sparse checkout. **E-11** the owner registry folds into E-02's declaration surface; the tracked marker is a MITIGATION, not proof, and the report says so — coupling flagged BEFORE building E-02 rather than after. **E-12** submodules are **not** scanned from here — a submodule is another repository with its own gate obligation, and scanning it would be the E-03 fiction inverted; the six are named as unscanned and this is a **stated gap, not a covered one**. **Evidence:** 12 mutants one at a time in a throwaway clone, 11 killed first pass, **M5 survived and was closed** rather than reported as 11/12; M3 (`is_dir` for `exists`) is a ONE-CHARACTER change that silently reinstates the project-root scan in EVERY worktree. **Five §7 assertions were satisfied by the fixture** (fourth through seventh in that family), now remedied mechanically. **No principal ruling.** Marker wiped a 20th time, restored onto the post-sign hash. Prior human-direct: `de4b812`. |
| 24 | r2.25 | `c8d32c7` | standing-delegation | 2026-09-17 08:15 | _(the commit carrying this row)_ | **§9 built E-02's declaration surface and found that "DECLARED" HAD BEEN A STATE THE CODE COULD DESCRIBE AND NEVER REACH.** `RENDERED_ARTIFACT_DECLARATIONS` was an empty frozenset nothing populated: **five** tests iterated it and ran zero times, **four** monkeypatched it, and the oldest had been vacuous since written because its "binary" fixture `b"\xff\xfe not utf-8"` decodes as **UTF-16** — the file was READABLE, so the assertion held with or without a declaration. **The behaviour the mechanism exists for had no test for four revisions.** **Four false-clean shapes the first real entry could have reached:** (1) **THE REGISTRY POLICED ITSELF** — placement was judged against the owner set defined in the very file whose declarations it constrains, so **delisting a project made its subtree unowned and therefore repo-root-declarable**: one edit, one file, another team's artifacts cleared, zero defects, demonstrated end-to-end against the shipped command (exit 2 → exit 0 with a payload present); (2) an **ABSENT** declaration file read as an **EMPTY** one, silently reverting to the pre-r2.24 marker-only mitigation with a healthy exit code — *E-02 reproduced inside the fix for E-02*, in a module that already says an unreadable file is not an empty one; (3) **dispatch payloads were declarable** — the one class never exemptible, and reusing the waiver's own regex would have exempted precisely `leak.pdf` because that pattern is `.md`-only; (4) a declared path could also be content-excluded — a missing RULE whose test was vacuous on both sides. **Six ways to get a traceback out of the one command that must not fail silently**, each exit 1 with no report: a non-UTF-8 byte (`UnicodeDecodeError` is a `ValueError`, not an `OSError`), a non-list `declarations`, mixed-type keys crashing THE UNKNOWN-KEY CHECK ITSELF, mistyped truthy fields, an unclosed handle in TWO readers, and `str(version)` accepting `0x1`. **12 MUTANTS SURVIVED THE ENTIRE 851-TEST SUITE**, including this work's own headline claim that a broken declaration fails the gate — every defect test asserted on the validator's RETURN VALUE and none on the EXIT CODE; 18 tests added, all 16 mutants re-run in a clone, zero survivors. **Rulings:** **E-13** a broken declaration file is **not** "could not scan at all" — treat nothing as declared (fail-closed), still render the listing, **exit 2** with the structural error naming the file; counts for declaration defects and undecodable files kept **separate** so the header adds up. **E-13b** assertions bind the **observable the consumer sees**, not only a return value — E-08's function-vs-command split reappearing as a testing habit. **E-14** declarations load **once through a snapshot**, ruled on the correctness half (a concurrent edit yielded a **self-contradictory single report**), not the 19 redundant parses; it lives in the guards module and owns the two owner sets. **E-15** every defect in an entry reported in one pass. **E-16** reviewer isolation gains a **per-reviewer** copy and a **verified interpreter**, because the shared `.venv`'s editable install points at the worktree — sixth in the ambient-state family; and `undecodable_unallowed`'s reach into the DrugBank exclusions is named as the one **non-mechanical** part of E6-6. **E-10's interpretation RATIFIED:** "fails only when this project owns it" means `failing_undeclared`'s predicate — ours **or** nobody's — because "unowned fails here" is load-bearing for E6-4 and a missing file is no different. **Delegation rule 13 added** from two instances in one day in two repositories: prose implying a check the tool does not perform is a false claim, and survives review *because* it reads as reassurance — `derived_from` "a claim a reader can check" (never verified), and `BioSimEnv`'s sealed-tested-metering docstring (component proven, `check()` never called). **The agent's ninth and tenth fixture vacuities are its own, written this session after adopting the rule that produced them.** **No principal ruling.** Marker wiped a 21st time, restored onto the post-sign hash. Prior human-direct: `de4b812`. |
| 25 | r2.26 | `302d23e` | standing-delegation | 2026-09-17 08:55 | _(the commit carrying this row)_ | **THIS REVISION EXISTS BECAUSE THE AGENT REFUSED TO IMPLEMENT A CLAUSE THAT DID NOT EXIST.** `grep -n "E6-7" build-plan.md` returned **nothing** at `c8d32c7`: E6-6 appeared four times, **E6-7 zero**. The CTO had written "E6-6/E6-7 remain yours" in four separate dispatches, and E6-7's only durable record was the phrase *"module extraction and a combined entry point with the FAIL in the API, authorised"* in **log row 20 — the record of an approval, not a source of conditions**. The agent's stated reason for stopping is the keeper: its wording for E6-7 had been reconstructed **across a compaction boundary**, so building to it risked the G-17/E-04 shape — implementing an unstated clause and later finding the words were its own. **An agent asked to implement a clause that does not exist is being asked to approve it.** It built the half needing no clause and stopped. **This is the project's own warning violated by the party who wrote it:** the plan already says *an invariant that lives only in prose is one a docstring can quietly contradict*, and E6-7 lived only in **dispatch** prose, which is not even in the repository's enforcement surface. **E6-7 now STATED:** one public entry point a **non-pytest consumer** can call, composing readability + declarations + ownership + accession content, with **the FAIL in it**, re-implementing none of the pieces, and following E-13's three-state exit contract — because today those four are composed by the **test suite** and the **report renderer**, so the invariant is enforced for whoever runs `pytest` and for nobody else. **Second clause, from the agent's own find: a production guard may not be a bare `assert`.** `assert_no_container_is_declared` enforced E6-2 with one; **`python -O` STRIPS asserts**, so the guard did not weaken — it **vanished**, and a declared readable container would be cleared in silence. It also raised `AssertionError`, a test-shaped exception, from production code. Now an explicit raise, enforced two ways: a test parsing the module's own source for bare asserts, and a test running under `-O` in a subprocess. The agent scanned every other production module rather than assuming; no others. **Two further self-catches:** E-15's fix made `dict(declaration_defects(...))` **lossy at 15 test sites** (a dict keeps the last defect, so assertions would have silently begun depending on check ORDER — the vacuity family again); and the E6-6 extraction's **first run refused to report**, because the moved file was untracked and §8's witness check declined to speak for a tree it could not see itself in — *the guard caught its own extraction*. **Delegation rule 14 added:** a condition an agent is expected to build must exist in the SIGNED PLAN, not in a dispatch, a handoff, or this log. **Marker restore broke its own frontmatter** — an unquoted-looking colon in the r2.26 conditions entry — and was caught by the standing parse check, not by `plan-gate verify`, which passes invalid YAML; same class as r2.12. **No principal ruling.** Marker wiped a 22nd time, restored onto the post-sign hash. Prior human-direct: `de4b812`. |
| 26 | r2.27 | `3ab0db9` | standing-delegation | 2026-09-17 10:35 | _(the commit carrying this row)_ | **§10 extracted the guard, built E6-7 to r2.26's clause, and showed that THE MOVE WAS CLEAN WHILE ALL THE RISK SAT IN THE BEHAVIOUR CHANGES.** A reviewer's **AST census over 98 pre-move definitions** found nothing lost, duplicated or meaningfully reordered — 49/49 `ingest` and 37/49 guard definitions byte-identical with all 12 differences accounted for, both predicates at the right call sites, zero guard dependency on `ingest`, live report byte-identical — and said so without hedging. The named non-mechanical seam was **two** questions, not one: `readability_waived` (dispatch payloads, NOT the ledger) and `content_exempt` (which does cover it). **The guard caught its own extraction:** the first run after the move REFUSED TO REPORT, because the new file was untracked and §8's witness declined to speak for a tree it could not see itself in. **TWO FAIL-OPEN DEFECTS, EACH UNDER A SENTENCE OF THE AGENT'S ASSERTING THE OPPOSITE**, in the iteration implementing rule 13: `ContentPolicy`'s *"both defaults refuse nothing, so a caller who forgets them gets a NOISIER gate"* is **false of `content_exempt`** — exempt nothing and the double-exemption defect never fires, the declaration HOLDS and the file is **CLEARED** (measured against the shipped policy); and the structural-error banner said *"more files fail, never fewer"* while beneath it a broken declaration file turned `owner=<unowned> [FAILS HERE]` into `owner=ghost-lib [listed]`, because `registry=None` read as "no registry yet" and answered with the WIDER set — **one YAML syntax error was the only difference between the runs**, inside the E-13 fix whose subject is exactly that. **r2.26's `-O` ENFORCEMENT SHIPPED VACUOUS AND THE CLAUSE WAS THE CTO'S:** the test raised an exception it had constructed itself and re-parsed source with `ast`, which yields `Assert` nodes identically under `-O`; a mutant making the refusal vanish **exactly and only under `-O`** passed all three related tests. Clause corrected — **start a child interpreter with `-O` and OBSERVE the guard refusing; observe behaviour, never re-derive it.** **The snapshot covered the YAML and not the verdicts:** `declaration_defects` ran three times per report, so a rebuilt artifact produced **one report that disagreed with itself** — the listing clearing a path the defects section called STALE in the same run; E-14's failure inside the fix for E-14. **22 mutants, TEN survived, three inside tests written to prove the property they mutate**; the absent-file test could not tell ABSENT from EMPTY; and nothing bound the policy the CLI passes — two mutants dropping it entirely passed all 895 tests, the old test having patched an attribute that **did not exist** (eleventh vacuity of the family); five more assertions became NO-OPS in the extraction, re-pointed textually onto a policy waiving nothing. **Rulings:** **E-17** a REQUIRED `ScanContext` with **no resolving fallback**, and the renderer split into DATA and PRESENTATION so **the exit code is assertable without parsing a string** — its living only in the renderer is *why* E-13b happened; its own iteration, after E-18. **E-18** `guards/decoding.py` and `guards/repo.py`, two pure moves that remove `ingest`'s reach into three PRIVATE guard names — a re-coupling of what E6-6 split; do FIRST. **E-19** measure `readability_waived` and **delete it if it never fires**, since an inert mechanism reads as coverage. **E-20** structural errors are carried by the **header counts**, not only prose — E-13's own rationale applied to the case E-13 created. **New clause:** two predicates with **opposite safe directions may not share a default**. **Delegation rule 15 added:** a mutation result is evidence ONLY if the baseline was green — the agent nearly reported "no survivors" from a clone whose baseline was already 8-failed, every mutant killed by the same failing test. It also disclosed **disarming a live test while fixing another finding**, the second time this session. **E-16 exercised for the first time and caught nothing, which is the intended answer.** **No principal ruling.** Marker wiped a 23rd time, restored onto the post-sign hash. Prior human-direct: `de4b812`. |
| 27 | r2.28 | `b63405a` | standing-delegation | 2026-09-17 12:10 | _(the commit carrying this row)_ | **§11 FAILED its gate with nine blocking findings, and the one that matters most implicates the CTO.** `record-content-report` composes **THREE** of E6-7's four checks — readability, declarations, ownership. `real_accession_hits` and `ledger_tuple_hits` are **UNREACHABLE FROM IT**, and `enforce_record_content` has **no caller outside its own test file**. **The CTO ran that command repeatedly, quoted `23 listed / N scanned / 0 failing / exit 0` back as independent verification, and recorded it as evidence the record-content invariant held. It is not that evidence.** The tree IS clean on accessions (live-repo scans at `test_record_content_guard.py:170` and `:244`), but that rests on `pytest` and was attributed to the command. **E6-5 — the CTO's own clause — is exactly the rule broken:** a mechanism enforcing three halves may not be cited for the fourth. **E6-7 declared UNMET.** **INDEX vs WORKTREE, escalated not fixed, and ruled:** the guard lists `git ls-files -s` (the INDEX) and reads `root/rel` from the WORKTREE; reproduced end-to-end in a throwaway repo — index held the payload, disk held clean text, guard read clean, **the commit would have carried the payload**. A commit gate reads the **STAGED BLOB**; divergence is **not** made a refusal, because that would break ordinary in-progress development; and the report must **state which copy it read** (E6-5 applied to *which bytes* rather than *which half*). Composition stated rather than left to be discovered: staged blobs always exist, so **E-10's unresolvable-path handling applies to the worktree-inspection mode only**. **A TRUE FALSE CLEAN, fixed:** `decoding.py` refused soft/external links among the **root group's members only** while `visititems` skips links at **every depth** — measured, same link top-level → `readable=False`, nested one group down → `readable=True`; an `h5ad` with its payload behind an `ExternalLink` one level in was READ, never listed, zero hits. Fixed with an **anti-overcorrection** test so "refuse every group" cannot pass. **Contract escapes ruled:** `sha256: null` passes the exactly-one-claim check (which tests key PRESENCE) then raises `KeyError` → exit **1**, a fourth state outside the ruled three; the scan invariant compares **truthiness** so `exit_code=1` constructs and renders; the header prints "failing this gate: 0" beside exit 2; and **`ScanRow.detail` is interpolated RAW**, so a **structurally valid declaration** rendered a forged clean-report line at column 0 — r2.24 escaped paths, and escaping one field but not its neighbours is the same error one column over. Also: `_MAX_DATASET_BYTES` bounds h5py vlen strings by **pointer width** (measured **128x** understatement, so a 64 MiB cap admits ~8.6 GB), and `list(_parquet_chunks(...))` accumulates every batch, **negating the batching bound entirely**. **THE AGENT'S MUTATION METHOD WAS WEAKER THAN CLAIMED AND ITS OWN REVIEWER CAUGHT IT:** "7/7 killed against a green 165-passed subset" was true, but the clone was **project-only**, silently deselecting **44** tests including the ownership and anti-vacuity ones most likely to kill those mutants; a reviewer rebuilt a self-contained checkout staging all 788 files, baselined **942/5 with no deselections**, and re-derived all seven — they do die. *The conclusion held by luck of mutant choice, not by method.* **Rule 15 AMENDED:** green baseline **and** no silent deselection, stated as counts. **Thirteenth vacuity, the agent's own:** a test keying on **annotation text** cannot see the edit a person restoring the convenience default would make — survived at 942 — in the file that guards that pattern three tests earlier; and **37 of 118** mutants survived, including **every bounded-read ceiling in `decoding.py`**, beneath a docstring claiming every bound was measured. **The agent also retracted a rule of its own** that the CTO had inherited: `guards/__init__.py` justified the exception's location as belonging "with the layer that RAISES it" — `repo.py` raises it 5 times, `record_content.py` 20; the real reason was cycle avoidance, and the stated rule was post-hoc. Its design reviewer **rejected two of the four escalations it had drafted** and it carried the reviewer's version. **Nothing pushed; no §11 receipt exists and the agent declined to sign one until the fold is done.** **No principal ruling in this revision.** Marker wiped a 24th time, restored onto the post-sign hash. Prior human-direct: `de4b812`. |
| 28 | r2.29 | `545645e` | standing-delegation | 2026-09-17 13:55 | _(the commit carrying this row)_ | **§11 GATED AND ACCEPTED** — `origin/lung-on-chipsim` `af0fc30` → `20e9edd`, `main` untouched at `df89f50`, receipt `1ea4db8` verified "1 of 23", Hash A `c810075` (the artifact that ENTERED review) → Hash E `1ea4db8`; 962 passed / 5 skipped. **E6-7 IS NOW MET, AND THE CTO VERIFIED IT BY FALSIFICATION RATHER THAN BY READING THE CALL SITE** — reading the code is precisely how the previous false claim passed. Disconnecting `enforce_record_content` on a throwaway copy kills `test_the_shipped_command_fails_on_a_real_accession_in_tracked_content`, a test that names the **command**; the binding is a fixture clean on the other three halves so exit 2 can only arrive via the accession half. **The agent carried its REVIEWER'S version over its own draft on three of four escalations and WITHDREW the fourth.** **Rulings:** **index-vs-worktree** confirmed as **§12**, not a tail-end fold — the CTO's proceed list was three items and this was not among them, threading two byte-source modes changes what the gate certifies, and E-10's handling becomes worktree-mode-only; the agent was right to ask rather than assume. **Error taxonomy: TWO classes**, not the three it drafted — "scan could not be performed" (exit 3) vs "declaration data unusable" (exit 2), which already differ in exit semantics and are today separated only by which call site catches them; a third class for topology buys nothing since **no caller treats a topology failure differently from a decoding one**. Its deciding evidence was *not* that `pytest.raises(RuntimeError)` is broad (a test defect with a test fix) but that `RecordContentScan.__post_init__` raises the same class for an **internal invariant violation**, surfacing as exit 3 *"your checkout could not be scanned"* for a programming error inside the guard. Exception moves to a **dependency-free `guards/errors.py`**, correcting a rule the agent wrote and the CTO inherited: `guards/__init__.py` claimed the exception belongs "with the layer that RAISES it" when `repo.py` raises 5 and `record_content.py` 20 — the real reason was cycle avoidance, the rule post-hoc. **Module split RE-ORDERED:** signatures and the root/surface binding **first**, then `errors.py` and `policy.py`, then the report — *splitting a module whose interior signatures are wrong exports the wrong signature*, five public functions still taking `(root, paths, policy, surface)` with the anti-vacuity invariant running on none of them; `policy.py` is promoted because `drugbank_snapshot` imports the entire ~1100-line guard to build a two-field dataclass. **Report grouping WITHDRAWN** by the agent: remedies are per-category, two dispositions give a filter rather than a grouping, and the real defect was the header count. **New clause: `derived_from` must pin the DECLARED file's bytes** — E6-3's letter was implemented and its substance was not, so the ban on a bare path is satisfied while the checkable claim a reader was promised is not. **Carried to §12 with reproductions:** surface not bound to the root it was read from and the memo key omitting root; the readability cache keeping a stale verdict across a same-size mtime-preserving rewrite; `git ls-files -s` emitting three records per path during an unresolved merge, inflating `tracked_count`; `ScanContext`/`DeclarationSurface` frozen+eq so hashing raises `TypeError`; and one path printing two DIFFERENT owners in one report. **The agent's receipt defect is one this repo already filed:** signing `--project lung-on-chipsim` while `git-safe-commit` verifies `--project bioFM` put a valid receipt in a bucket nothing looked in — section (b) of the 2026-09-16 receipt-verification report, now independently reproduced; it also disclosed testing a staging hypothesis twice before checking its own argument. **The test-constant item remains with the principal, untouched and unlooked-at.** **CTO process failure disclosed:** the first marker restore used an UNQUOTED heredoc, so the shell executed every backticked identifier and wrote a marker full of holes; caught by reading the result, same class as the `sed` delimiter collision that put a literal placeholder into dispatch #115. **No principal ruling.** Marker wiped a 25th time, restored onto the post-sign hash. Prior human-direct: `de4b812`. |
| 29 | r2.30 | `2235d01` | principal + standing-delegation | 2026-09-17 15:20 | _(the commit carrying this row)_ | **PRINCIPAL RULING on the test constant: REPLACE WITHOUT INVESTIGATING.** The agent measured first as ruled and returned two findings. (1) A synthetic constant can **never** exercise the positive detection path — `REAL_ACCESSION_RE` excludes `DB9xxxx` by negative lookahead — which vindicated its original worry. (2) **The guard is SHAPE-ONLY**: it holds no list of assigned identifiers and cannot distinguish an assigned accession from an unassigned one of the same form, so an obviously-unassigned value exercises the mechanism identically. **CTO-verified by probing only unassigned values:** two values in the permitted synthetic range → no hit, two obviously-unassigned shape-valid non-synthetic forms (an all-zeros and an all-ones five-digit body) → HIT, wrong digit counts → no hit. _(Corrected in place 2026-09-17 with disclosure: this row originally wrote those two probe values as LITERALS, which the accession half correctly flagged — the guard is shape-only and cannot know an all-zeros identifier denotes nothing. The remediation's own record reintroduced the pattern the remediation removed. Forms described, never written.)_ **Because the remediation costs ZERO coverage it is strictly dominant**, so the principal ruled it applied **without anyone determining whether the existing pair is an association** — that determination being the association the constraint forbids. Nobody looked; it stays forbidden after the change. Replaced at both sites (`fa8f81d`), suite **962/5 unchanged**, which is itself the confirmation that no detection test depended on the identifier denoting anything. **A CTO INSTRUCTION WAS WRONG AND THE AGENT REFUSED THE WRONG HALF.** The CTO ruled "drop the fragment-assembly and the comment explaining it" **without establishing why the assembly existed**. Measured: the guard's own test file is **NOT excluded** from the accession scan, and a **literal** shape-valid non-synthetic form matches — so any literal there turns the live gate **RED** regardless of value. *The assembly was never the problem; the value was.* CTO re-verified independently: `is_accession_excluded(<test file>)` → `False`, literal match → `True`, live gate exit 0 at 790 tracked after the change. The agent also rejected the alternative of excluding the test file from the scan, on the grounds that **excluding the file that tests the scan is a bigger hole than an assembled constant** — correct, and the shape of defect this iteration kept finding. The comment now carries **both** reasons. **New clause:** the test file is deliberately in scope and its assembled constant is load-bearing, written down because the next person to tidy it will have the same instinct the CTO had. **Delegation rule 17 added, identified by the agent about its own work:** *the answer that confirms your prior is where the search stops — so keep going.* Its first finding answered the question as asked and confirmed the worry it already held, and it nearly stopped there; the finding that changed the decision was the second. **Provenance symmetry recorded:** the agent could not verify this principal ruling independently — it arrived through the CTO's session — so it treated it as a work instruction, disclosed that in the commit body, and kept the change reversible, exactly as the CTO did when the agent cited a ruling the CTO could not see. Marker wiped a 26th time, restored onto the post-sign hash. Prior human-direct: `de4b812`. |
| 30 | r2.31 | `797fc99` | standing-delegation | 2026-09-17 16:40 | _(the commit carrying this row)_ | **THE ACCESSION HALF'S FIRST LIVE CATCH, AND IT CAUGHT THE CTO.** Recording the test-constant ruling in row 29, the CTO wrote **two shape-valid non-synthetic literals** into this very log — probe values denoting nothing, but the guard is **shape-only by design and must not know that**, so the live gate went **RED at exit 2** on tracked coordination content. *The record of the remediation reintroduced the pattern the remediation removed*, one file away from where the defect had just been fixed. **The agent reported it without reproducing the value** — naming the file rather than quoting it — and **declined to edit** a plan-gated file it did not own. Both correct. **E6-7 EARNED ITSELF HERE:** before §11 the shipped command composed three of four halves and would have reported this tree **CLEAN at exit 0** — the exit 0 both parties had been citing as compliance evidence; the half wired in §11 is the half that caught this, on the coordination content of the person who ruled it. **RULING: the exclusion set is NOT widened.** `workstreams/**/plan/**` stays in scope — excluding the coordination files that record the guard's own decisions is the same hole as excluding the test file that tests the scan, ruled against one revision earlier, and would blind the gate to exactly the surface where its rulings are written. **The fix is not to write accession literals into tracked prose at all**; describe the FORM, never the value. **Enforced on the COMMIT PATH** (`plan-gate` / the coordination-commit route), **not** by adding paths to the guard's exclusion set, because the decisive evidence is that remembering failed immediately — the literals were written by the author of the rule in the act of writing it down. **Remediation:** row 29 corrected in place with disclosure, forms described; `plan-approval.md` and `build-plan.md` swept and already clean; the six flagged files under `.claude/usr/**/dispatches/` are pre-`#122` `.md` messages covered by the **E6-4 waiver by design**, not new exposure. Plan hash was unchanged by the log edit (`build-plan.md` untouched), so **no re-sign was required for the correction itself**. **§12 step 1 landed meanwhile:** the surface is bound to the root it was READ FROM, `root` dropped from all eight signatures that took it beside `surface`, and the mismatched pair is now **unrepresentable rather than merely unchecked**. **The agent disclosed a non-idempotent migration it ran twice**, costing 32 already-fixed call sites a second argument — caught by **reading a rewritten line rather than by the failure count**, reverted to HEAD and redone once: *a migration that is not idempotent is one that must not be re-run, and it did not say so.* **No principal ruling.** Marker wiped a 27th time, restored onto the post-sign hash, and verified to contain **zero** accession literals. Prior human-direct: `de4b812`. |
