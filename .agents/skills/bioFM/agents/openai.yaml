interface:
  display_name: "BioFM"
  short_description: "Repo-specific patterns and workflows for bioFM"
  default_prompt: "Use the bioFM repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true